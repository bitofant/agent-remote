# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Everything in CLAUDE.md must be extremely terse: bulleted lists, not paragraphs, as concise and token-efficient as possible.**

**Keep this file correct: update it when details change, and add genuinely important architectural decisions as they're made.**

**Code comments must be extremely terse too: short one-liners explaining *why*, never restating the code. No prose blocks, no redundant JSDoc.**

## Project state

- Scaffolded. Working vertical slice: launch Claude Code/pi sessions, stream live to xterm in the browser, type back, switch between concurrent sessions.
- TypeScript throughout. Tests use **Vitest** (`npm test`), co-located `*.test.ts` next to source, Node environment (`vitest.config.ts`). First coverage is the pure shared logic (`shared/chat.test.ts` reducer, `shared/render.test.ts`) — no DOM, no processes, no tokens. Test files are excluded from `npm run typecheck` (both tsconfigs) so that gate stays scoped to shipping code; Vitest runs them. Component tests (jsdom + Testing Library) and live smoke scripts layer on top later.
- **Prefer TDD when adding a new harness surface or normalized message type** — i.e. a new `ChatEvent` / `ChatAction` / `ChatUiRequest` kind, an adapter mapping a fresh SDK/harness signal, or a new agent interaction flow (permissions, dialogs, resume, compaction, plan-mode/`ExitPlanMode`, model/mode switches). These have a clean *observable backend contract* (the emitted events / folded `ChatState`) that a token-free `claude-local` `*.e2e.test.ts` can pin. Order: **draft the test first** against the intended contract → **implement** the adapter/protocol → **run it live and adjust the test** to what the real harness actually does (e.g. an empty `plan` field, messages the SDK does/doesn't replay) → **only then build the frontend**. Keep the deterministic assertion (our event surfacing) as the hard gate and any model-behavior check (recall, verbosity) as a softer secondary one.

## Commands

- `npm run dev` — single-process dev server (`tsx watch server/index.ts --dev`).
- `npm run build` — production frontend build to `dist/web` (Vite).
- `npm start` — run server serving the prebuilt `dist/web`.
- `npm run typecheck` — `tsc --noEmit` for web (`tsconfig.json`) + server (`tsconfig.server.json`).
- `npm test` — Vitest run (once, pure/fast — no processes/tokens); `npm run test:watch` for the watch loop. Excludes `*.e2e.test.ts`.
- `npm run test:e2e` — live end-to-end tests (`*.e2e.test.ts`, `vitest.e2e.config.ts`) that drive the real `claude-local` harness against vLLM; self-skip when claude-local is disabled/unreachable. Kept out of `npm test` (needs a running endpoint) — as is anything else that spawns processes, even the endpoint-free `manager-exit.e2e.test.ts` / `system.e2e.test.ts`.
- `npm run chat-log-report` — offline chat-render-log diagnostics (see chat render log below).
- Requires `config.json` (run `./config-gen.sh`); the agent CLIs (`claude`, `pi`) must be on `PATH`.
- Server is **never compiled** — `npm start` runs `tsx server/index.ts` directly. Only the frontend is built (`dist/web`).

## Deployment (systemd user service)

- **⚠️ NEVER run `./restart.sh`, `./rebuild.sh`, `./stop.sh`, or any `systemctl --user restart/stop agent-remote` yourself.** The agent (you) runs *inside* this application — restarting/stopping/rebuilding the service kills all live sessions, including your own. When a change needs to be picked up, **ask the user to run `./restart.sh`** themselves; never do it (or the rebuild) on their behalf.
- `./install-service.sh` — one-time: writes/enables `~/.config/systemd/user/agent-remote.service` (runs `npm start`), builds `dist/web` if missing, enables linger. User service (not system): agent CLIs live in per-user paths.
- `./start.sh` / `./stop.sh` / `./restart.sh` wrap `systemctl --user` as the canonical prod runner. `./restart.sh` rebuilds frontend then restarts.
- `./start.sh dev` is the exception: runs the `tsx watch` HMR server directly (pidfile + `setsid`, not systemd). `./stop.sh` prioritizes that pidfile if present, else stops the service.
- `./rebuild.sh` builds frontend only. Frontend change → rebuild + restart; server-only change → plain `systemctl --user restart agent-remote` (no build needed).
- **Restart resilience** (restart.sh used to leave the service dead; three compounding causes, all fixed — don't regress any):
  - `rebuild.sh` **stages + atomically swaps** (`vite build --outDir dist/web.next` → two `mv`s). Vite's `emptyOutDir` used to wipe `dist/web` under the live server for the whole build.
  - `serveStatic` (`index.ts`) **try/catches the read** → 503, and the server has `uncaughtException`/`unhandledRejection` log-and-continue handlers. A missing `index.html` mid-rebuild threw ENOENT out of an async handler and killed the process (and every live session). The SPA fallback path is not `existsSync`-checked, so it can always vanish under us.
  - `restart.sh` runs the rebuild+restart in a **transient unit** (`systemd-run --user --collect --unit=agent-remote-restart`) so it's outside the cgroup systemd is tearing down — the caller is normally an agent session *inside* the service, so an inline build gets killed half-way. It also verifies the unit came back and re-`start`s it if not. Falls back to inline when systemd-run/the unit is absent. (This is why `nohup ./restart.sh` used to be needed; it isn't now.)
  - Unit has `Restart=always` + `StartLimitIntervalSec=0` (default 5-starts/10s budget let a crash-loop wedge it dead permanently) + `TimeoutStopSec=20`.

## Layout

- `shared/protocol.ts` — WS message + session types, shared by both sides. Harness-agnostic. Also `CommandListing` (the `/api/commands` response shape), `SessionEvent` (command-start/end + cwd events), `FileEntry`/`DirListing`/`FileContent` (the file-editor `/api/files` + `/api/file` shapes), and the normalized chat schema (`SessionUi`, `ChatState`/`ChatMessage`/`ChatPart` — incl. the `image` part variant, `ChatEvent`, `ChatAction` — `prompt` carries optional `images:ChatImageRef[]`, `ChatUiRequest`/`ChatUiOption`, `ChatImageRef`, `ChatCapabilities`/`RewindPreview`, `AutoPrompt`/`ContinuityNewSession`, `AgentRun`). **Card options are structured** (`ChatUiOption {value,label,detail?,intent?}`): `value` is the stable id round-tripped in `ui-response.value` and matched by the adapter, `label`/`detail` are display-only (so a harness can enrich button text without breaking the decode), and `intent` (`accept`/`always`/`reject`/`cancel`) carries the semantics so `ChatView` and `assistant.ts` never pattern-match on label strings (AI-mode picks `accept`, never `always`).
- `shared/chat.ts` — `applyChatEvent` reducer folding `ChatEvent`s into `ChatState`; run identically by server (replayable per-session state) and client (live updates). Caps: 200 messages, 20k/tool output, 20 notices, 30 sub-agent runs. The only *destructive* fold is `rewind` (see below) — everything else appends/patches.
- **System turns (`ChatMessage.role: "system"`).** A turn the conversation *received* but neither party authored — a background-task notification, a peer/channel message. It's part of the transcript (it's what the agent replied to) but must never be attributed to the user. Renders as a muted centered line in transcript order (`.chat-system`, same idiom as `.chat-notice` but ordered, not pinned to the bottom); folded by the ordinary `user-message` event, which for `role:"system"` **skips clearing `promptSuggestions`/`autoPrompt`** — a notification is not the developer replying, and must not spend Continuity Mode's armed prompt. `suggestions.ts` labels it `System:` (never `Assistant:`, or the model reads machine output as its own words).
  - **The dangerous half is rewind, not the misattribution.** The claude CLI injects these as `user` entries with prose, indistinguishable from a prompt unless you check provenance. Counting one as a prompt desynchronizes `promptIds` from `promptUuid`'s positional fallback, and a rewind then forks the session at the *wrong* turn. Hence: system turns never enter `promptIds`/`promptUuids`, and the positional filter applies the same predicate.
- **Sub-agent runs (nested chat sessions).** A tool call that spawns an agent (claude's `Agent`/`Task`) carries its whole nested conversation in `ChatState.agents[toolId]` (`AgentRun {toolId, agentType?, description?, state: ChatState, loading?}`). Three envelope events — **`agent-start`** (create/merge metadata, idempotent: a repeat never resets a live transcript), **`agent-event {toolId, event}`** (fold `event` into that run's `state` via `applyChatEvent` *recursively* — no second copy of the streaming/part-delta/tool-matching logic), **`agent-done {toolId, report?}`** (clear `loading`; append `report` as a final bubble unless the transcript already ends with it). Design rules that are load-bearing:
  - **The map is FLAT and root-only.** A depth-2 agent gets its own top-level entry keyed by a toolId that happens to live inside a depth-1 nested transcript; the renderer looks up the same root map, so arbitrary depth needs no recursive data shape and the reducer never searches nested states for a key. A nested `state.agents` stays `{}`.
  - **`rewind` prunes it transitively** (`reachableAgents`): BFS the surviving root messages' tool parts, then those runs' own tool parts. Same spirit as the `assistantTraces` prune.
  - **An empty run falls back to the ordinary tool view** (both `renderPart` and `ChatView`'s `ToolPart`), never an empty panel — a backgrounded agent's tool output is a "launched successfully" stub, which is the only information there is until its text arrives. `loading` is the one empty state that renders a panel ("Loading transcript…").
- `shared/render.ts` — single source of truth for turning a `ChatPart`/`ChatMessage` into displayed HTML (`renderPart`/`renderMessage`, plus the `md`/`escapeHtml`/`argsPreview`/`toolGlyph`/`truncate` primitives). `ChatView.tsx` imports the primitives so the UI can't drift from what's logged; `server/chatLog.ts` calls `renderMessage` to capture what's shown. Imports `marked` — keep it imported only by `ChatView` on the web side so it stays in the lazy chunk.
  - **Tool-call display** is driven by `toolView(part)` (field-driven, harness-agnostic — covers claude's `Edit`/`Read`/`Write`/`Bash` and pi's lowercase `read`/`write`/`bash` by looking at `file_path`/`path`, `command`, `old_string`/`new_string`, `content`, not tool names): returns a `{primary, secondary?, body}` display model. Summary shows a legible subject (`shortenPath` → `…/dir/file.ts`, or the command) not raw JSON; body is a Git-style diff for edits, a code block for Write/Bash content, path+range for Read, or pretty JSON for unknown tools. Both `renderPart` (HTML) and `ChatView`'s `ToolPart` (JSX) consume `toolView` so they can't drift. Tool bodies use `white-space: pre` + horizontal scroll (mobile pans, never wraps per-column). **An assistant turn renders as a *column of bubbles*, not one** (`groupParts` in `render.ts`, consumed by both `ChatView`'s `Bubble` and `renderMessage`): consecutive prose parts share a bubble, each tool call is its own (`.chat-tool.standalone` — `--panel` fill + `--r-lg`, i.e. bubble weight, with `overflow:hidden` clipping its full-bleed bodies). A turn is nearly always all prose or all tool, and the old single-bubble layout made the common tool-only turn a raised card inset inside a near-empty bubble; a mixed turn now just yields several bubbles. Nested `.chat-tool` (raised card, `--r-sm`) survives for the permission card, which is why `standalone` is a modifier rather than a change to the base rule. **This is presentation only** — grouping happens at render time, never in the reducer, since `ChatMessage.id` is load-bearing for rewind (`promptIds`), `AssistantTrace.anchorMessageId`, the `chat_render_log` key, and the streaming→finalized handoff. Prose groups key off their first part's index, tool groups off `toolId` so `<details>` open state survives a growing turn. Empty `thinking` parts (claude never streams reasoning) render as a transient pulsing "Thinking…" label and are stripped by the `shared/chat.ts` reducer (`dropEmptyThinking`) once the next part starts or the turn ends.
  - **Diff display model**: `ToolBody {kind:"diff", path?, rows:DiffRow[], added, removed}` + `ToolView.stat?` (the collapsed `+N −M`, rendered by both `renderPart`'s summary and `ChatView`'s so the render log matches the UI). `DiffRow = {kind:"line",line} | {kind:"fold",count?}` — consumers never re-derive folding. Pipeline: `editPairs()` (field-driven, so claude's `old_string`/`new_string` and pi's `edits[]:{oldText,newText}` both yield hunks; an empty side is a pure insertion/deletion) → `pairLines` → `lineDiff` (minimal LCS) → `markWordDiff` (token-level `.diff-word` marks inside changed pairs) → `diffRows` (elide unchanged runs into `.diff-fold` rows, `DIFF_CONTEXT = 4`). Folding is applied **per hunk**, and a hunk break row is only pushed when no fold already borders the join (two adjacent `.diff-fold` rows are noise). Three guards against folding too much: a diff with no changed lines at all, a fragment ≤ `context*4` lines, and a hidden run of <2 lines all stay fully visible. No line numbers — `old_string` carries no file offset, so any gutter number would be a lie. `.diff-fold` deliberately gets no red/green bar (see styles).
  - **Thinking amount** (`thinkingSummary(text, live?)` → `{label, amount}`): collapsed thinking shows how long the thought was, words until `WORDS_PER_PAGE * PAGES_FROM` (500 × 1.2) then pages with one decimal (`Thought for 420 words`, `Thought for 2.0 pages`). `live` swaps the label to "Thinking…" for the part still being streamed — `ChatView` passes it only for the last part of the last group, never for finished prose.
  - **A tool call with a sub-agent run expands into that agent's own chat session** (`.chat-agent`, rendered by `renderAgentPanel` / `ChatView`'s `AgentPanel`) *instead of* its args JSON and its `<pre>` output — the sub-agent's answer is already the last nested bubble, so the `<pre>` would just duplicate it. Same `Bubble`/`groupParts`/`ToolPart` path as the main transcript, so it nests recursively. **The panel opens with the task the agent was given** (`agentPrompt(part)` — the tool's own `args.prompt`), because *neither* path carries the sub-agent's first user message: the live stream doesn't forward it and `getSubagentMessages` drops it as the sidechain root (verified). Render-time synthesis, never in the reducer — same rule as `groupParts`. The panel is **`max-height:50vh` with its own scroll** — an agent run is a whole conversation and must not push the main transcript off-screen — and pins to the bottom while the parent tool is still running (independent of `.chat-scroll`'s stickiness). Elevation: the panel sits on the tool bubble's `--panel`, so its own bubbles/tools step up to `--raised` (no fourth surface needed — tool bodies already `color-mix` off `--raised`), and nested tool bodies drop from 40vh to 20vh so they can't fill the panel. `ChatView` threads the run map through an **`AgentsContext`**, not props: `ToolPart` → `Bubble` → `ToolPart` nests arbitrarily deep. Expanding a `<details>` whose run is an empty stub fires the `load-agent` action (see resume, below).
- `server/` — Node backend. `config.ts` (loads `config.json`), `auth.ts` (password/cookie auth — see below), `llm.ts` (optional LLM assist — see below), `suggestions.ts` (harness-agnostic synthesized next-prompt suggestions — see below), `turnRouter.ts` (where a settled turn goes — see below), `autopr.ts`/`prAgent.ts`/`git.ts` (the auto-PR capability), `continuity.ts` (Continuity Mode), `adapters/` (harness boundary, incl. `shell-integration/` injected rc scripts), `sessions/manager.ts` (PTY/streaming/lifecycle + session-event fan-out, harness-agnostic), `commands.ts` (lists cwd executables + `$PATH` + shell aliases + recent/frequent commands from the DB command log; PATH/alias cached 30s), `files.ts` (file-editor backend: list subfolders / read / write, every path confined to a folder root), `browse.ts` (add-folder picker backend — see below), `system/` (System state telemetry — see below), `chatLog.ts` (chat render log capture — see below), `paths.ts` (`expandHome` — see below), `db.ts` (SQLite: folders, users, auth sessions, command log, resumable chat sessions, chat render log), `index.ts` (HTTP + WS + dev Vite).
- **Session ↔ folder membership:** `SessionInfo.folder` (launch cwd, fixed for the session's life) is the *only* thing sessions are grouped by — `App.tsx`'s `sessionsInFolder`, the auto-select-new-session effect, `manager.sessionFolder` (folder-recency bumps), `chat_sessions.folder`. **`SessionInfo.cwd` is live and drifts with every `cd`** (shell integration) and is display/logging only (`sessionCwd` → command log, session-row subtitle). Grouping by `cwd` was a real bug: `cd` out of the launch folder made the session vanish from the UI (its cwd matched no known folder, while it stayed running and mounted), then re-attach itself to whatever folder was *added later* at that path. Membership must never depend on mutable state. The active-tab pin (`activeSession[folder]`) is likewise validated against the folder's own tabs before use — terminals are all mounted at once and shown purely on an id match, so a foreign pin paints another folder's session over an empty tab list.
- **Folder path normalization (`server/paths.ts`):** a folder path is an *identity* (the `folders` primary key, and what `SessionInfo.folder` is compared against to group a session under its folder), so it has exactly one spelling: `normalizeFolder` = `expandHome` + a guaranteed trailing slash. Two spellings of one folder = two list entries that drift apart, each with its own sessions/command history.
  - `expandHome` expands a leading `~`/`~/…` (never `~user/…`, needs a passwd lookup). `~` is shell syntax — Node's `fs`/`spawn` treat it as a literal directory name, so a folder typed as `~/src/foo` never resolves (nothing to do with the service user: it's a systemd *user* service, so `HOME` is already the installing user's).
  - Applied at the WS entry points in `index.ts` (`start` cwd, `addFolder`, `removeFolder`) so the allowlist / session cwd / `/api` cwd params see one form — **and again inside `db.ts`'s `upsertFolder`/`removeFolder`**. The DB one is load-bearing, not belt-and-braces: one un-normalized write anywhere forks the folder (this is exactly how a `~` row bred a duplicate — opening it upserted the *expanded* session cwd as a second row). **Normalize at the persistence boundary, never per call site.**
  - `manager.ts` normalizes the shell-integration `cwd` event too — a shell reports `/x/y` where the folder key is `/x/y/`, which would drop a session out of its own folder tab.
  - `db.ts` `migrateFolderPaths()` (runs at import, idempotent) folds legacy rows into canonical form: merges into an existing canonical row keeping the newer `last_used_at`, and for `~` rows re-points `chat_sessions.folder` / `commands.cwd` / `chat_render_log.cwd` (slash-only fixes don't re-point — those columns hold live cwds, incl. subdirectories, whose trailing slash is their own).
  - Client-side, `submitNewFolder` never predicts the stored path: it defers selection (`pendingFolderSelect`) to the next `folders` broadcast, where the add sorts first.
  - **Display is the other half:** storage is canonical/absolute, but the UI shows paths home-relative (`~/src/x/`). The `folders` server message carries `home` (server `$HOME`) alongside the list — the browser can't know it — and `web/paths.ts` `displayPath(path, home)` renders it (folder list + folder header; `title=` keeps the absolute path). Home *itself* is the exception — it stays absolute (`~/` alone names nothing) and `folderName(path, home)` labels it "home" rather than the account name. Display only: every path sent back to the server is the stored one. Pure coverage `web/paths.test.ts`.
- **Add folder (folder picker):** the sidebar's "+ Add" opens `FolderPicker.tsx`, a modal filesystem browser (reuses the `.resume-*` dialog shell) mounted at app level in `App.tsx` — adding a folder must work with none open. **The text field is the source of truth; the tree is a view of it** (ancestors auto-expanded, best prefix match highlighted), and every tree interaction writes back into the field, so typing/Tab/tap all converge on one string. Typed text splits at the last `/` into a directory (listed) + a partial name (prefix-filters its subfolders, case-exact first); ↑/↓ move the highlight, Tab completes it into the field with a trailing `/`, Enter adds (or, when the partial isn't a real folder, completes instead — Add is disabled and "No such folder." shown only once the parent listing proves it missing). Path text logic is pure in `web/folderPath.ts` (`splitTyped`/`toAbsDir`/`toTyped`/`parentOf`/`rootFor`/`chainFrom`/`matchEntries`, tested in `folderPath.test.ts`): display keeps `~/` (here home itself collapses too, unlike `displayPath`) while tree keys and requests are canonical absolute paths, and the tree roots at home when the path is inside it (else `/`). Backend is `GET /api/browse?path=` → `server/browse.ts` `browseDirs` (auth'd; `~`/empty = home; directory *names* only, symlinked dirs followed, 1000-entry cap with `truncated`, fs errors mapped to readable text). Deliberately **not** folder-allowlisted, unlike `/api/files` — you're picking a folder that isn't known yet, and typing a path by hand always reached just as far. Submitting still sends the raw text: the server normalizes it (`normalizeFolder`) and `pendingFolderSelect` adopts whatever it stored.
- `web/` — React app. `client.ts` (WS client, per-session output buffering + per-session `ChatState`, sticky Ctrl modifier applied in `input()`), `auth.ts` (client-side auth helpers), `Login.tsx` (login/signup gate), `TerminalView.tsx` (xterm per session), `ChatView.tsx` (chat-bubble view for `ui:"chat"` sessions, lazy-loaded, bundles `marked`), `FileEditor.tsx` (the file-editor tab: file picker + CodeMirror, lazy-loaded), `SystemState.tsx` (the System state page, lazy-loaded — see below), `App.tsx` (workspace + mobile keyboard key-bar + editor-tab bookkeeping + AI-assistant mode toggle), `CommandBuilder.tsx` (the `./` command-builder dialog), `FolderPicker.tsx` + `folderPath.ts` (the add-folder browser — see below), `commandCatalog.ts` (static well-known-command subcommand/flag data + curated common-command list), `styles.css`. Becoming the active tab focuses that session's input (xterm / chat composer) so it's typeable at once; while an exited session is active, Enter/Escape trigger "Close session" (`App.tsx` window keydown, suppressed while a dialog is open).
- **Last view (server-persisted, per user):** which folder + tab you were on survives a reload and follows you across devices. `ViewState {folder, sessionId}` (`shared/protocol.ts`) ↔ `user_views` table (one row per user, folder normalized in `setUserView` like every other folder column) ↔ `GET/PUT /api/view` (auth'd, keyed by `authedUser`). **Load-time only, deliberately not over `/ws`** — a live broadcast would yank an open laptop tab around when you tap something on your phone. `App.tsx`: fetch on mount → `storedView` ref; a one-shot restore effect waits for `viewFetched` + `snapshotSeen` (the first WS `sessions` message; the server sends `folders` before it) so ids can be validated. **`client.onSessions`/`onFolders` replay their current value synchronously on subscribe**, so the connect effect's `replaying` flag ignores that first (empty) call — counting it as the snapshot made the restore run against an empty folder list and silently fall through to the default every time. The restore is also ordered *after* the auto-select-new-session effect, which treats the whole first batch as new and would otherwise win. `viewRestored` gates both the "default to most-recent folder" effect (else it races the stored folder) and the debounced save (else the default overwrites what was stored). Stale ids just fall back to the folder's default tab — sessions die, and editor tabs are client-only so their ids never survive a reload.
- **Chat sessions (pi + claude):** `SessionInfo.ui` is `"terminal" | "chat"`, declared by the adapter. Two adapter shapes drive chat, both harness-confined; the manager just folds their `ChatEvent`s into per-session `ChatState`:
  - **`createChatTranslator()` (pi)** — manager spawns the agent **piped, no PTY** (`child_process.spawn`); the `ChatTranslator` maps wire format ↔ schema (`push(stdout) → ChatEvent[]`, `encode(ChatAction) → stdin bytes + synthetic events`). pi runs `pi --mode rpc` (JSONL, manual `\n` framing, never readline — U+2028/29). ALL pi vocabulary in `adapters/pi.ts`.
  - **`createChatSession()` (claude)** — the adapter owns the process/SDK and exposes `start(handlers)`/`action(ChatAction)`/`close()`; the manager drives that interface (no child/translator). `adapters/claude.ts` wraps the **Claude Agent SDK** (`@anthropic-ai/claude-agent-sdk`, `query()` with a pushable async-generator prompt stream). This is what buys **feature parity with the TUI**: `canUseTool` → permission cards (`ui-request` select Allow/Deny, plus "Always allow" when the SDK supplies `suggestions`; resolved by `ui-response`). **Always-allow:** the SDK's `suggestions` are *alternatives* (the TUI's separate buttons), not a bundle — a Bash call yields an `addRules` **plus** an `addDirectories` **plus** a `setMode`, so `alwaysAllowUpdates()` returns only the rule ones as `updatedPermissions` (sending the lot would also flip the session to acceptEdits and grant a directory off one click). The CLI persists them at its own suggested `destination` — in practice `localSettings`, i.e. it creates/updates the project's `.claude/settings.local.json` (`{"permissions":{"allow":["Bash(touch notes.txt)"]}}`) — so the rule survives restarts; we never write that file ourselves. `describeSuggestions()` renders the rule + destination as the option's `detail` (`Bash(touch notes.txt) · saved to local settings`) so the button names what it grants instead of being a blank cheque. Edit tools instead get "Allow all edits" (session-scoped `acceptEdits` flip, no rule). Pure coverage `claude-permissions.test.ts`; live `claude-permission.e2e.test.ts` asserts the option shape, the detail text, and that the settings file really gains the rule. The card carries the tool as `ChatUiRequest.tool` (`{name, args}`), NOT a pre-summarized `message` string, so `ChatView` renders it through the shared `toolView` (same diff/code/path view as the transcript, expanded) instead of raw arg JSON. **Plan mode:** in `permissionMode:"plan"` Claude drafts a plan and calls the `ExitPlanMode` tool instead of executing; the adapter special-cases it into a distinct `ui-request` of `kind:"plan"` (the plan markdown rides in `message`, options `["Accept plan","Keep planning"]`). Accept → allow the tool (exits plan mode) **and** `setPermissionMode("acceptEdits")` so the follow-through auto-accepts edits; Keep planning → deny the tool with the typed instructions as the message (model stays in plan mode and revises). `ChatView` renders the plan as a bordered markdown card (`kind:"plan"`) with an "Accept plan & auto-accept edits" button + a refine-instructions box; the LLM auto-decider ignores `plan` (accepting a plan is always human-driven). `onUserDialog`+`supportedDialogKinds:["permission_ask_user_question"]` → the **AskUserQuestion** tool as a structured `ui-request` of `kind:"questions"` (1–4 questions, each 2–4 label+description options, `multiSelect`) instead of a raw permission card; the answers ride back as a PermissionResult `updatedInput.answers` (question text → chosen label(s), multi joined `", "`), mirroring the TUI. Other dialog kinds aren't declared → CLI applies its default, `setModel`/`supportedModels` → model switcher (**menu policy in `adapters/model-menu.ts`**, pure/tested: `menuLabel` appends the resolved version to alias rows, `pruneToLatest`+`sortModels` curate latest-per-family, `current` derived from the shown rows. **A bracketed suffix is a variant, not a version** — the enterprise catalog lists `claude-opus-4-8` *and* `claude-opus-4-8[1m]` (same model, 1M context, drawn from usage credits), so `isVariant` gives it its own pruning group (both survive; each prunes its own older versions), strips the tag before parsing the version (else `claude-sonnet-5[1m]` reads as 5.1), and sorts it after its plain row. **`pickDefault(menu, DEFAULT_MODELS)` (not `includes()`) picks the starting model** from an ordered alias preference — today `["opusplan","opus"]`, i.e. Opus Plan Mode where the catalog offers it (work/enterprise), else plain Opus (private). Ranking: earlier alias wins outright → exact alias row / versioned id of that family / mere substring → plain before `[1m]` → highest version. A substring scan can't express this (`"opusplan".includes("opus")`), which is why it matched whichever sorted first. Behaviour aliases like `opusplan` are unversioned, so pruning always keeps them in the menu. **`SEED_MODEL` (`"opus"`) is separate**: `query({model})` is seeded before the catalog is known, so `loadControlInfo` then `setModel`s to the resolved pick when it differs — otherwise the header would claim Opus Plan Mode while the CLI ran plain Opus (behavioural, not cosmetic). That switch has its own try/catch so a catalog refusing it still yields a model list + slash commands and keeps reporting what's actually running; the pick is stored on `this.model` so a rewind relaunch keeps it. No filtered/unfiltered UI toggle — showing both the plain and `[1m]` rows is the whole point), `setPermissionMode` → mode toggle (a fixed `modes` event lists Default/Plan/Allow-all-edits/Auto; `set-mode` action → `setPermissionMode`), `supportedCommands` → slash-command palette (all fetched eagerly on start, before the first prompt). SDK message mapping mirrors the raw stream-json shapes (stream_event deltas → part events; `input_json_delta` accumulates tool args, emitted at `content_block_stop`; `user` tool_result → `tool-end`; `result` → busy false). **Compaction:** the SDK `system`/`compact_boundary` message (manual `/compact` or auto when the window fills) → an info `notice` (`Compacted context (pre → post tokens) [manual|auto]`) so the transcript marks the boundary instead of silently swapping history for a summary; a `system`/`status` with `compact_result:"failed"` → an error notice. `permissionMode: "default"` = auto-allow read-only, prompt (via `canUseTool`) for the rest. Abort = `query.interrupt()`. **Sub-agents:** `forwardSubagentText:true` on `query()` makes the CLI forward a sub-agent's *whole* conversation (without it only its tool_use/tool_result blocks come through — a heartbeat); every SDK message variant carries `parent_tool_use_id`, and the assistant/user ones add `subagent_type`/`task_description`. The pure `subagentMeta(msg)` reads that, and `emitFor(parent, event)` wraps everything the message-building helpers emit into an `agent-event` when a parent is set — so `handleStreamEvent`/`replayAssistantMessage`/`replayUserMessage`/`handleToolResults` serve both threads on one code path. Three things this must get right, all found live:
    - **Streaming state is per-thread** (`streamCtx(parent)` over a `Map`, `""` = main). `toolBlocks` is keyed by stream block *index* and `streamed` guards the whole-message echo; a single copy means two concurrent sub-agents collide and a sub-agent's `message_start` clears the main turn's blocks.
    - **Rewind identity stays main-thread only.** `promptIds`/`promptUuids` pushes and `pairPromptUuid` are skipped when `parent` is set — a sub-agent's prompts aren't rewind targets and would shift every positional lookup. Likewise `busy` only flips on the main thread (a sub-agent runs *inside* the turn).
    - **The Agent tool's `tool_result` is NOT the report.** For a backgrounded agent it's only the "Async agent launched successfully…" stub (the answer arrives later via `system`/`task_notification`), and for a foreground one the forwarded conversation already ends with the answer. So `agent-done`'s `report` comes only from `task_notification.summary`; deriving it from the tool result put the launch stub at the *top* of the nested transcript. `system`/`task_started` (`tool_use_id`+`subagent_type`+`description`) is the earliest `agent-start`, with a lazy fallback on the first message carrying an unknown parent.
    **Provenance is asymmetric — this cost two wrong fixes, so don't "simplify" it.** A background task's completion is injected into the conversation as a `user` entry, and how you detect it differs by path: the **live** `SDKUserMessage` carries **no `origin`** (verified — its keys are `type/message/parent_tool_use_id/session_id/uuid/timestamp`), while `getSessionMessages` likewise **drops `origin`** from `SessionMessage`. So neither SDK surface can classify it. Live, the signal is the structured `system`/`task_notification` (`tool_use_id`+`summary`+`status` → `taskLine`); on **resume**, the CLI *does* write `origin:{kind:"task-notification"}` to the JSONL, so `parseOrigins` scrapes the file itself (`sessionFile` via the same `projectPath` probe as the sidecar) and `isHumanTurn` reads that. A notification whose `<tool-use-id>` belongs to a known run is **suppressed on replay** (`notificationToolId`) — live it became that run's report inside the nested transcript, so emitting it again out here would make a resumed session disagree with the one it restores. Everything degrades to "treat as human" (today's behaviour) if the file can't be read.
    **`pairPromptUuid` matches on TEXT, not arrival order**, for the same reason: since the live echo carries no `origin`, a blind "oldest unpaired prompt" pairing hands a notification's uuid to a real prompt and corrupts the rewind target. `promptText` holds what we sent; no match → no pairing → the (provenance-filtered) positional fallback.
    **Resume is lazy, not eager**: `GetSessionMessagesOptions` has no `includeSubagents`, and sub-agent transcripts live in their own files (`~/.claude/projects/<key>/<sessionId>/subagents/agent-<id>.jsonl` + a `.meta.json` sidecar). `replayHistory` only emits **empty `agent-start` stubs** (that's how the client knows a bubble is worth expanding); expanding one sends the `load-agent` `ChatAction`, and `loadAgent` folds `getSubagentMessages` through the same replay helpers with `parent` set. The `toolUseId → agentId` join exists *only* in the sidecar — `listSubagents` gives ids but never the tool call — so `readAgentMeta` scans the project dirs for the session rather than reproducing the CLI's cwd-mangling. Best-effort throughout: a read failure just clears `loading` and the bubble keeps its plain tool view. Pure coverage `claude-subagent.test.ts` (`subagentMeta`/`parseAgentMeta`/`isHumanTurn`/`systemTurnText`/`taskLine`/`notificationToolId`/`parseOrigins`) + `shared/chat.test.ts`; live `claude-agent.e2e.test.ts` — two tests: the live path (hard gate: a run's transcript never leaks into the main one, ends with the sub-agent talking, and contains no launch stub; soft gate: that the small model reaches for the Agent tool at all), and the **resume path** (a resumed session yields a metadata-carrying empty stub, and `load-agent` fills it — pinning the sidecar join end-to-end). **Next-prompt suggestions:** `promptSuggestions:true` on `query()` makes the CLI emit a `prompt_suggestion` SDK message after each turn (never the first) with a predicted next user prompt → mapped to a `prompt-suggestion` `ChatEvent` (wrapped as a **single-element** list — the schema `ChatState.promptSuggestions` is `string[]`; Claude only ever emits one) → folded into `ChatState.promptSuggestions` (cleared on the next `user-message`). `ChatView` shows them as dashed chips above the composer (idle + empty composer only; wrap horizontally, capped at two rows); a tap prefills the composer with the full prompt for review (never auto-sends). The suggestion is a *secondary CLI model call* — works against the real Anthropic endpoint but the **claude-local** harness emits none (small model errors on it; SDK suppresses suggestions after API errors), so the deterministic mapping/fold is pinned by pure reducer tests and the live e2e only best-effort-probes it. Claude declares `HarnessAdapter.nativePromptSuggestions:true` so the generic synthesizer (below) skips it; harnesses without native suggestions (pi) get 1–3 synthesized server-side into the same `ChatState.promptSuggestions`.
  - **Billing:** claude stays flat-rate subscription — the SDK drives the Claude Code CLI on the user's OAuth creds. Never set `ANTHROPIC_API_KEY` or `--bare` (forces metered API) **on the production `claude` harness**. Verified on subscription.
  - **CLI binary (local vs bundled):** the Agent SDK ships its *own* version-pinned native `claude` (an optional dep, `@anthropic-ai/claude-agent-sdk-<platform>`) and spawns it unless `pathToClaudeCodeExecutable` is set. That bundled copy can lag the CLI the user installed, so its **model catalog trails** (symptom: `/model` shows fewer models in the web UI than the standalone TUI — e.g. no "Opus 5"). `createClaudeAdapter` therefore resolves the harness's `cfg.command` (default `claude`) on `$PATH` **once at startup** via `resolveLocalCli()` (returns an absolute native-binary path; rejects `.js`/`.mjs`/… so the SDK spawns it directly, not via node) and threads it to `ClaudeChatSession` → `query({pathToClaudeCodeExecutable})`; missing → SDK falls back to its bundled binary (logged either way). Keep the SDK version bumped too so its message-mapping protocol stays compatible with a newer local CLI. Pure coverage: `claude-cli.test.ts`.
  - **`claude-local` (token-free e2e testing):** an optional second instance of the *same* Claude adapter (`config.harnesses.claudeLocal`, id `claude-local`, built in `registry.ts`), whose `HarnessConfig.env` points the Claude CLI at a local Anthropic-compatible endpoint. vLLM natively serves `/v1/messages`, so `env` sets `ANTHROPIC_BASE_URL=http://localhost:8000` + `ANTHROPIC_API_KEY/AUTH_TOKEN=dummy` + `ANTHROPIC_DEFAULT_{OPUS,SONNET,HAIKU}_MODEL=<served model>` (`CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1` to stop catalog pings). `createClaudeAdapter(cfg, {id,name})` takes the id/name override; `env` threads to `ClaudeChatSession` → `query({env})`. **Gotcha:** the SDK `env` option *replaces* the subprocess env, so the adapter spreads `process.env` first (keeps PATH/HOME). This exercises the whole real stack (SDK message mapping, permission cards, streaming, resume, WS/ChatState/ChatView) with zero Claude tokens — the dummy key is scoped to this harness's env only, never the production `claude` path. vLLM model must have tool-calling enabled (`--enable-auto-tool-choice --tool-call-parser …`) and ≥32K context. `scripts/claude-local-smoke.ts` drives one turn through the adapter as a quick check. Automated coverage: `server/adapters/*.e2e.test.ts` (run via `npm run test:e2e`, shared helpers in `claude-local.testkit.ts` — `claudeLocal()`/`endpointUp()`/`ChatDriver`) — `claude.e2e.test.ts` drives a real edit turn and asserts the backend asks permission (Edit → `ui-request`) and parses the streamed tool args (`input_json_delta` → well-formed Edit object); `claude-resume.e2e.test.ts` plants a codeword, resumes by key, and asserts the prior turn is replayed from disk AND the model recalls the codeword (context restored, not a fresh session); `claude-compact.e2e.test.ts` drives `/compact` and asserts the backend surfaces the compaction boundary as a notice (deterministic) plus a softer check that the model still recalls the pre-compaction codeword from the summary; `claude-plan.e2e.test.ts` enters plan mode, refines the proposed plan (stays in plan mode), then accepts it and asserts the session switches to `acceptEdits`; `claude-permission.e2e.test.ts` drives a prompting Bash call (use `touch`, NOT `echo` — safe commands are auto-allowed and never reach `canUseTool`), asserts the card's option values/intents + the always-choice's rule detail, then answers "Always allow" (`ChatDriver.answerWith`) and asserts the CLI wrote the rule into the project's `.claude/settings.local.json`; `claude-suggestion.e2e.test.ts` asserts enabling `promptSuggestions` keeps turns healthy and best-effort-folds a `prompt-suggestion` if the harness emits one (local usually doesn't — see above); `claude-agent.e2e.test.ts` prompts the model to delegate to the Explore sub-agent and asserts the nested transcript folds correctly (see the sub-agent paragraph above). All fold through the shared reducer/renderer and self-skip unless claude-local is enabled and its endpoint answers. **pi resume** has its own live coverage: `pi-resume.e2e.test.ts` (helpers in `pi-local.testkit.ts` — `piLocal()`/`endpointUp()`/`PiDriver`, which drive a real `pi --mode rpc` subprocess through the production `SessionManager`, with pi pointed at the same local vLLM via its own config, zero tokens) plants a codeword, resumes by the manager-minted key, and asserts (a) the prior turn is rebuilt from disk by the adapter's `replayHistory` and (b) the model recalls the codeword — checking *all* reply parts incl. reasoning, since the small model interleaves its final text answer. The e2e config runs files **serially** (`fileParallelism:false`) — one vLLM endpoint mustn't be hammered by concurrent turns. Not a substitute for an occasional real-token smoke of OAuth/model-switcher behavior.
  - Manager mirrors busy→`currentCommand` (pulsing dot unchanged; the `index.ts` command recorder skips `ui==="chat"` so prompts stay out of `./`-builder recents). On WS connect the server sends one `chatState` snapshot per chat session (scrollback replay); live `chatEvent`s follow. `ChatView.tsx` renders bubbles (markdown via `marked`, raw HTML escaped), collapsible thinking/tool parts, permission/UI cards (confirm/select/input/questions/plan), per-user-bubble rewind buttons (see Rewind), a model `<select>` + permission-mode `<select>` + slash-command palette (shown when `ChatState.models`/`.modes`/`.commands` are non-empty), queued prompts, next-prompt suggestion chips (`.chat-suggestions` wrapping `.chat-suggestion`, from `ChatState.promptSuggestions`, 0–3, wrap capped at two rows; tap prefills the composer with the full prompt even when the chip is truncated), and a composer (Enter sends; Send↔Steer while busy + Stop=abort). **Slash-command typeahead:** a draft that is exactly `/<no-space>` (`slashQuery`) auto-opens the palette and prefix-filters it (`menuCommands`, client-only `/resume`+`/rewind` first); ↑/↓ move the highlight (`.chat-command.highlighted`), Tab completes to `/<name> `, Esc dismisses until the next keystroke (`menuDismissed`). The menu is **purely draft-driven** (`commandsOpen` = live `slashQuery` + not dismissed; no manual-open state): the `/` button — composer-inline or key-bar, same `!keyboardOpen` swap as the attach button — is just a *key* that types a slash at the caret (`typeSlash`), so mobile reaches the palette and its typeahead filtering through the same path as the keyboard. Terminal-only UI (terminal key-bar, select mode, builder) gated off via `activeIsChat`. ChatView has its **own** mobile key-bar (`.chat-key-bar`, gated on the `keyboardOpen` prop from `App.tsx`'s `keyboard.open`) above the composer with keys the soft keyboard lacks/hides for prompt-writing — currently attach-image (left-most), `/`, backtick, newline — inserting at the textarea caret via `insertAtCursor` (reuses `.key-button`). Manually running `pi`/`claude` inside a Terminal session stays raw PTY. New chat harness = adapter with `createChatTranslator` *or* `createChatSession`; no UI/manager changes.
- **Resume (chat sessions):** harness-agnostic via an opaque "resume key". `SessionOptions.resume` (input) → adapter restores prior state; `ChatSessionHandlers.onResumable(key)` (output) → manager persists it. Both **claude** and **pi** implement resume; **terminal** ignores it. Two mechanisms by adapter shape:
  - **claude (ChatSession)** — the adapter owns identity: passes `resume` to `query()` (restores the model's context) and reports the SDK `session_id` from the `system`/`init` message via `onResumable`.
  - **pi (ChatTranslator)** — the *manager* owns identity (the translator contract has no session concept). The adapter sets `resumable:true`; in `startChat` the manager mints a stable id (`opts.resume ?? randomUUID()`), threads it into `invocation()` via `SessionOptions.resume`, and reports it via `onResumable` (fresh and resumed alike). pi's `invocation` turns it into `--session-id <id>` (creates if missing, reloads+restores context if present — same flag both ways). pi's RPC stream, like claude's SDK, does NOT replay prior turns on reload, so the adapter rebuilds the transcript from pi's on-disk session JSONL (`~/.pi/agent/sessions/<mangled-cwd>/*_<id>.jsonl`; `mangleCwd` = `--`+segments joined by `-`+`--`, with a full-scan fallback since ids are UUIDs) via the optional `ChatTranslator.replayHistory(opts)` hook — the manager calls it on resume and folds the returned events (`message` lines → user/assistant/toolResult → same ChatEvent vocabulary) into `ChatState` before the first new prompt. Best-effort: unreadable history yields an empty transcript (context is still restored). All pi session-file knowledge stays in `adapters/pi.ts`.
  - (claude gotcha) The SDK does NOT stream the prior conversation back on resume (nothing is emitted until the first new prompt, and even then only the new turn — no `isReplay` messages). So the adapter rebuilds the visible transcript itself in `replayHistory()` by reading the on-disk JSONL via the SDK's `getSessionMessages(sessionId, { dir: cwd })`, folding those whole messages through the same `replayAssistantMessage`/`replayUserMessage`/`handleToolResults` helpers (same ChatEvent vocabulary). The `streamedThisTurn` flag (set on stream `message_start`, cleared on `result`) still guards live delta-built turns against the SDK's redundant whole-message echo; the `isReplay` branch in `handleMessage` is now a vestigial safety net (the SDK doesn't send those). `db.ts` `chat_sessions` table (keyed by resume key + folder, title = first user prompt) survives restarts; `index.ts`'s server-global subscriber persists on `onResumable`/first `user-message`. `GET/DELETE /api/resumable?cwd=|key=` (auth'd + folder-allowlisted) list/forget; the list hides currently-live keys (`manager.liveResumeKeys()`). UI: resume is triggered from **`/resume` in `ChatView`'s slash-command menu** (mirrors the CLI), shown when the folder has resumable sessions. `/resume` is a **client-only** menu entry (not a harness `state.commands` command): `ChatView` takes `canResume`/`onResume` props; picking it calls `onResume` (not `insertCommand`), and `send()` intercepts a `/resume` draft the same way (Tab-completing it then Enter opens the picker instead of prompting the harness). The dialog state itself stays folder-level in `Workspace` (`resumable`/`resumeDialogOpen`), so `onResume={() => setResumeDialogOpen(true)}`. It opens a modal dialog listing the folder's resumable sessions **filtered to the active chat tab's harness** (`resumableForActive` in `App.tsx` — resuming a claude session in a pi tab, or vice-versa, is nonsense; `canResume` is likewise per-harness); picking one calls `client.start(harnessId, folder, resumeKey)` and closes the dialog, `×` forgets one. Reachable only when a chat tab is active (no standalone/sidebar entry).
- **Composer draft (server-side, harness-agnostic):** the unsent prompt lives in `ChatState.draft`, so a reload / folder detour / other device doesn't lose half-written text (the chat analogue of a terminal's unsubmitted line sitting in the PTY — terminals need nothing). `set-draft` `ChatAction` → handled by `manager.ts` itself like `set-assistant` (never an adapter) → `draft` `ChatEvent` → reducer. Text only; attachments stay client-side. Not persisted to SQLite — a server restart kills the session anyway.
  - Sending consumes it: the manager clears the draft on **every** `prompt` action, unconditionally (the `if (session.chat?.draft)` around it only skips a no-op broadcast — the sender normally cleared it already). A prompt sent is a draft spent; don't reintroduce a conditional clear (an earlier prefix-match rule tried to protect text typed *after* Send, since `index.ts` resolves prompt images async and so applies an image prompt *after* later `set-draft`s — that window is worth less than the simple rule, and `ChatView` keeps the local draft authoritative anyway).
  - `ChatView` keeps the local `draft` authoritative for the caret and syncs on a 300ms debounce (+ an unmount flush + an explicit clear on send). Two guards make the echo safe, and both were real bugs: it adopts a server value only after the server has **acked our latest push** (`acked` ref — an in-flight echo would otherwise resurrect a just-sent prompt), and only when the composer is **empty or unfocused** (an outright "never while focused" rule broke the main case: on a reload the snapshot arrives *after* mount, by which time the composer is already focused). `index.ts`'s chat-render-log subscriber skips `draft` events alongside `part-delta`/`tool-update` — they touch no message.
- **Rewind (claude only):** jump the conversation back to an earlier **user** prompt — it and everything after it leave the transcript *and* the model's context; the agent is interrupted; the prompt goes back into the composer for re-sending. Gated by `ChatState.capabilities` (`ChatCapabilities {rewind?, rewindFiles?}`, a `capabilities` event emitted at session start), so pi shows none of it and needs no changes. **Manager/`index.ts` are untouched** — `rewind`/`rewind-preview` are `ChatAction`s that pass straight through to `chatSession.action`, and the adapter emits the resulting `rewind`/`rewind-preview` events itself.
  - **Message identity** is the crux: `ChatState` user-message ids are `randomUUID()`, unrelated to SDK transcript uuids, so `ClaudeChatSession` maps them itself — `promptIds` (ordered ids of every `user-message` it emitted, live + replayed) plus `promptUuids` (exact map). Exact uuids come from `replayHistory` (`m.uuid`) and, live, from the CLI's **echo** of our prompt in `handleMessage` `case "user"` (`msg.uuid`, paired with the oldest unpaired prompt; prose distinguishes it from tool_result-only messages). `promptUuid()` falls back to **position** among the transcript's prose user entries when the exact uuid is unknown — which is the normal path after a rewind, since `forkSession` remaps every uuid (hence `promptUuids.clear()` on relaunch). The e2e test rewinds twice specifically to cover that fallback.
  - **Mechanism:** no `Query` control request truncates history, so a rewind is a **relaunch**: `q.interrupt()` → optional `q.rewindFiles(uuid)` (gated on `enableFileCheckpointing:true`, now always set; must run on the *live* query — forks carry no undo history) → `forkSession(sessionId, {dir, upToMessageId: <entry before the prompt>})` → `teardown()` → `launch({resume: forkId, restart: true})`. `start()` was split into `start()` + `launch()`; `restart` skips `replayHistory` (clients keep their truncated transcript) and `loadControlInfo` (would reset the model selection). Model/mode are tracked on the instance so the relaunch keeps the user's picks. A **generation counter** (bumped on relaunch, compared in `consume`) makes the superseded query's abort unwind quietly instead of emitting an error notice and firing `onExit` (which would mark the session exited).
  - Forking means the **pre-rewind conversation stays resumable** (it reappears in `/api/resumable` once it's no longer live). **The CLI only emits `system`/`init` once a prompt is pushed** — so `sessionId` (the fork target) can't wait on it: `launch()` adopts `opts.resume` immediately, and the rewind adopts the fork id (`sessionId` + `onResumable(forkId)`) right after relaunching. Deriving it from `init` alone made *resume → rewind before the first prompt* fail with "session not ready" (the common case: reopen a tab, undo the last turn) and left the stale key persisted; both are pinned by e2e tests.
  - **UI** (`ChatView.tsx`, all per-session — unlike `/resume` nothing is lifted into `App.tsx`): a faded `↺` button left of every user bubble (`Bubble` gains `onRewind`; the bubble is wrapped in `.chat-turn.user`, which takes over its flex sizing — `.chat-rewind` is `opacity:.3` not `0`, so touch can reach it) and a client-only `/rewind` slash command opening a picker of prior prompts. Both land in the same `RewindConfirm` dialog (`.resume-*` shell + `.rewind-dialog`), which fires `rewind-preview` on open and shows the dry run beside an "also restore files" checkbox. Confirming prefills the composer (text + image refs) *before* dispatching `rewind`, since the truncation drops the message. The client-only-command hack generalized: menu entries carry an optional `run()` (`/resume` and `/rewind`) instead of `/resume`'s old `resume?: boolean` flag; `send()` intercepts both bare drafts.
  - Coverage: `shared/chat.test.ts` (fold), `server/adapters/claude-rewind.e2e.test.ts` (live claude-local: truncation + re-key, double rewind, context retained/dropped by codeword, dry-run leaves the tree alone, `restoreFiles` restores an edited file). `ChatDriver.flush()` now also runs on `onResumable` — a rewind re-keys without emitting further events.
- **Chat render log (diagnostics):** to review/improve how chat messages are displayed, `server/chatLog.ts` persists, per finalized chat message, both the original normalized `ChatMessage` and its rendered form (`shared/render.ts`'s `renderMessage`: per-part component name + CSS class + produced HTML). Captured harness-agnostically in `index.ts`'s server-global `manager.subscribe` (`onChatEvent`, skipping the high-frequency `part-delta`/`tool-update` — final form lands on assistant-end/tool-end; for an `agent-event` the *inner* event is what's judged, since it's as frequent as whatever it wraps); deduped by a per-message JSON signature before any rendering (cheap to call often; the signature also folds in each referenced sub-agent run's size, else a growing nested transcript never refreshes the row), cleared on `onRemoved`. A tool part with a nested transcript logs as component `AgentPart`. `db.ts` `chat_render_log` table keyed by `(session_id, message_id)`, upserted so late tool results refresh the row, capped ~20k rows (probabilistic prune). Read-only review via `GET /api/chat-log?session=&limit=` (auth'd; returns rows with `original`/`rendered` parsed from JSON). No UI/client involvement — pure server-side diagnostics. **`npm run chat-log-report`** (`scripts/chat-log-report.ts`) is the offline CLI over the same table: summary (part/component/tool/status tallies) + heuristic issue flags; modes `--dump=<msgId>` (full original+rendered JSON), `--issue=<name>`, `--tool=<name>`, `--part=<type>`; filters `--harness= --session= --limit= --samples=`. Add a heuristic = one entry in its `ISSUES` map. Use it instead of ad-hoc SQLite scripts.
- **File editor / Files tab:** a fourth tab type alongside claude/pi/terminal, added from the same `+` menu ("Files"). NOT a harness — it's a **client-only tab** tracked in `App.tsx` (`editors` state), never by the PTY session manager; it's backed by REST, not `/ws`. Two steps in `FileEditor.tsx`: a lazy folder tree (file picker) then a CodeMirror 6 editor with a Save button (Ctrl/Cmd+S). Reads/writes via `GET /api/files?cwd=&path=` (dir listing), `GET/PUT /api/file?cwd=&path=` — all auth'd + folder-allowlisted like `/api/commands`; `server/files.ts` confines every path to the folder root (blocks `..`/absolute escapes), refuses binary/oversized files. CodeMirror + language packs are code-split (dynamic `import()` behind `React.lazy`) so they load only when an editor tab is opened. Save creates new files too (via the picker's "New file"). Terminal-only UI (key-bar, command builder) is gated off when the active tab is an editor (`activeIsEditor`).
  - **Upload/download** ride on the same picker rather than a tab of their own — it's already the per-folder, allowlist-scoped view of the files. `GET /api/download?cwd=&path=` (`Content-Disposition` attachment, RFC 5987 `filename*` for non-ASCII) and `POST /api/file-upload?cwd=&path=[&overwrite=1]`, both auth'd + folder-allowlisted, both **streamed** (`openDownload`/`saveBinaryFile` in `files.ts`) so a large artifact never buffers in memory — deliberately NOT via `readBinaryBody`, which buffers whole.
  - These skip `readTextFile`'s binary/2 MB refusals **on purpose**: those are *editor* constraints (a browser text field), not filesystem ones. Don't "fix" the asymmetry by loosening `readTextFile`.
  - **Not** `/api/upload` — that's the owner-scoped image blob store for chat prompts (`data/uploads/`, 4 image types, `uploads` table). Filesystem transfer and agent-attachment are different features that must not share a route.
  - Upload targets `selectedDir` (set by expanding a directory row, `""` = root) and the button names it (`Upload to src/`), so the destination is never implicit. An existing name returns **409** rather than clobbering; the client confirms and retries with `overwrite=1`. Multi-file via the hidden `<input multiple>`, plus drag-drop on the tree (desktop).
  - Per-file-row `↓` is an `<a download href>` — the same-origin HttpOnly cookie authenticates it, so no token plumbing. It sits at `opacity:.4` **always, never hover-only**: touch has no hover (same reason as `.chat-rewind`).
  - **Media preview:** picking an image/video in the tree previews it instead of erroring with "File appears to be binary.". The allowlist is `shared/media.ts` (`mediaKindFor`/`mediaTypeFor` — jpg/jpeg/png/gif/webp/avif/bmp + mp4/m4v/webm/mov), shared so the client's "is this previewable?" and the server's content-type can't drift. **`.svg` is deliberately excluded** — it's text and stays editable in CodeMirror. `FileEditor`'s `openFile` branches on it *before* the fetch, so a media click never round-trips through `readTextFile` (which would read the whole file just to refuse it).
    - Bytes come from **`GET /api/media?cwd=&path=`** — auth'd + folder-allowlisted like the rest, streamed via `openDownload`, **inline** disposition (`contentDisposition(name, "inline")`) with a real content-type from a **closed** allowlist: an unknown extension is a 400, never an octet-stream fallback, so a file in a user folder can never be served as `text/html` on our origin (this is also why it doesn't reuse `CONTENT_TYPES`, which serves `dist/web`). Plus `nosniff` + `default-src 'none'; sandbox`, and `no-store` because the agent rewrites these files. **Not** an `inline=1` flag on `/api/download` (that route stays attachment-only — every `↓` depends on it) and **not** `/api/upload` (owner-scoped chat blob store, different feature).
    - **`Range`/206 is load-bearing**: Safari won't start a `<video>` without it, and seeking needs it everywhere. `parseRange(header, size)` in `files.ts` (pure, tested) → `openDownload(root, rel, range)`, whose `size` stays the *full* file size for `content-range`.
    - Videos `loop` by default (previews are usually short clips worth re-watching); if auto-advance-to-next is ever added it has to replace looping, not join it.
    - **Gallery navigation** (`siblings`/`stepPreview` in `FileEditor.tsx`): previewable files in the *same directory*, in tree order, from the already-loaded `dirs[dirOf(path)]` listing — no extra request. Clamped, never wrapping. Driven by ←/→ on desktop and a **swipe** on touch, whose start point must fall in `SWIPE_ZONE` (top-left, `{width:.5, height:.75}` of the preview box) — the carve-out dodges the native video control strip at the bottom (which eats horizontal drags for scrubbing) and the close button top-right. Handlers sit on the `.editor-preview` **wrapper, deliberately not an overlay element**: an overlay would swallow taps meant for the media's own controls. Sub-`SWIPE_MIN_PX` or mostly-vertical drags are never claimed, so tap-to-pause and vertical scroll are untouched. Since the gesture is invisible, `.editor-preview-counter` (`3 / 12`, top-left, fades after 2s) is what announces the gallery exists — **don't drop it without adding another affordance**.
    - Layout is **CSS-only at the existing 640px breakpoint**, one DOM node: `.editor-panel` is a row of `.editor-main` (the picker/editor column) + `.editor-preview`, which is the right half on desktop and a fixed full-screen scrim (`z-index:45`, above the mobile sidebar, below dialogs) at ≤640px. `object-fit:contain` on a 100%×100% box scales both ways while keeping aspect. Preview and open doc are **mutually exclusive** — opening/creating a text file clears it; Escape, the corner `×` and a backdrop click dismiss it.
  - Path confinement for the transfer functions is pinned by `server/files.test.ts` (escape via `..`, via an absolute path, via a NUL byte; that a rejected write leaves the outside file untouched; and that a *ranged* read can't escape either — `/api/media` adds no confinement of its own).
- **System state page (host/GPU/LLM/docker telemetry):** a single sidebar link **above** Folders opening a folder-independent, read-only page of cards — System usage, GPU usage, LLM usage, Docker, Disk, Network. Backend is `server/system/` + one route; frontend is `web/SystemState.tsx` (lazy, default export). Linux-only by construction.
  - **One cached snapshot, one route.** `GET /api/system` (auth'd, no params) is a *cache read* with no `await` — structurally the same as `/api/llm-status` → `llmStatus()`. `systemSnapshot()` does no I/O, so the route has no async rejection path; it's gated by `authedUser` like every sibling because the snapshot names host processes and containers.
  - **The GET is the heartbeat.** `noteSystemWatcher()` starts the sampler on the first request and each tick stops it once nothing has asked for `IDLE_STOP_MS` (15s). So a closed page costs nothing, N tabs cost exactly one sampler, and there's no WS subscription bookkeeping. The client polls every 2s and skips while `document.hidden`. `startSystemSampling(config)` at boot only seeds one cheap `/proc` sample (so the first GET isn't empty) and starts no timers.
  - **Every section is independently nullable** (`SystemSnapshot` in `shared/protocol.ts`): `cpu`/`memory`/`disks`/`network`/`gpu`/`engine`/`containers` are `T | null` plus an `errors` map keyed by section. A keyed map, not a field per section — an absent section has no object to hang a reason on, and a `{data,error}` wrapper would force every consumer through `.data`. `tick()` is a `Promise.allSettled` of per-section try/catches building a **fresh** object published in one assignment, so a GET mid-tick never sees a half-frame. **Invariant (pinned by the e2e): an `errors` entry only ever accompanies a `null` section.**
  - **Rates are `number | null`, never a fake 0.** Everything shown as a rate (CPU %, tok/s, B/s, disk busy) is a delta of a monotonic counter, so `delta.ts`'s `rateOf`/`cpuPercent`/`histogramAvg` return `null` on the first sample, on `dt<=0`, and when the counter went **backwards** (reboot, engine restart, `docker restart`, iface bounce) — then reseed so the next window recovers. The UI prints `—`. Showing 0% for "we don't know yet" is a lie users notice.
  - **Pure parse ⟂ impure read.** Each module exports `parseX(text)` (unit-tested, no fs/spawn) plus a thin `readX()`/`probeX()`. That's what keeps `npm test` process-free; the readers are covered by `system.e2e.test.ts`, which asserts only the *contract* (a snapshot always returns, percentages in range, the errors invariant) and no values, so it passes on a box with no GPU/docker/engine.
  - **`/proc` over `os.*`:** `os.freemem()` is misleading (use `/proc/meminfo` `MemAvailable`) and `os.cpus()` omits `iowait`/`steal` while needing the same two-sample diff. **hwmon indices are NOT stable across boots** — a chip is always located by reading each `/sys/class/hwmon/*/name` (`pickHwmon`), cached 60s. There is **no** `/sys/class/thermal/thermal_zone*` on this box, so nothing may fall back to it.
  - **GPU→container attribution:** `nvidia-smi --query-compute-apps` reports **host** PIDs even for containerized processes; `containerFromCgroup(/proc/<pid>/cgroup)` yields the docker id (64 hex), matched against `docker ps`'s 12-char short ids with `full.startsWith(short)`. No `docker exec`/`docker top`. Only docker ids are matched — podman/k8s ids don't join to our rows.
  - **Cost control:** `docker stats` blocks ~1s, so it runs on its own 10s timer with its own `inFlight` flag and is merged into the `docker ps` rows by id (`ContainerSection.statsAt` lets the UI grey stale numbers); `docker ps` runs every 5s; `nvidia-smi` has an `inFlight` guard and keeps the previous frame rather than blanking. A probe that fails with ENOENT/permission-denied backs off for 5 minutes (`Probe` class), so a box without `nvidia-smi` or `docker` costs one failed spawn per 5 min, not one per tick.
  - **LLM engine discovery follows `config.llm.baseUrl` — no new config block.** Local → probe it and name the docker container publishing its port; **not local → assume there's no local engine** (`detection:"remote"`). `parseEngineTarget` strips a trailing `/v1` because **`/metrics` lives at the server root, not under the OpenAI prefix**. vLLM and llama.cpp are two flavours normalized behind one shape; a metric a flavour lacks is `null`, never 0, so a rename upstream degrades one row to `—` instead of throwing. Labels are parsed, never assumed away (vLLM tags everything `{engine="0",model_name=...}`, so `sumOf` sums across label sets).
    - **Detection validates CONTENT, never status codes, and demands POSITIVE evidence.** Open-WebUI (port 8080 here) answers **200 + HTML for every path** including `/metrics` and `/props`, *and* serves a real `/health` returning `{"status":true}`. So the order is: `/metrics` passing `looksLikePrometheus` **and** a known `vllm:`/`llamacpp:` prefix → `/v1/models` parsing as JSON with a string `data[0].id` (the parse *is* the validation) → llama.cpp's `/props` carrying `n_ctx`/`model`. **There is deliberately no bare `/health` probe** — a 200 there proves something is listening, not that it's an LLM engine, and it made Open-WebUI report as a running engine (found live). `parsePropsBody` likewise rejects JSON with no recognisable field.
    - A configured-but-silent endpoint yields a **non-null section carrying `EngineSection.detail`** ("Nothing answering on …" / "Container X publishes 8000 but isn't serving an engine API"), *not* an `errors.engine` entry — the section still says which endpoint was probed and which container publishes it, which is the useful signal. `errors.engine` is reserved for "no section at all" (missing/unparseable `baseUrl`).
  - **UI:** the sidebar link reuses the `.folder-item`/`.folder-item-open` pair (hover/`.active`/two-line layout for free — **zero new sidebar CSS**) and clears `sidebarOpen` like `openFolder`, which in turn clears `systemOpen`. The page reuses `.folder-header` for its title row, so the mobile `padding-left:52px` hamburger clearance comes free. **The folder subtree is hidden with `display:none` (`.main-view`), never unmounted** — all tabs are mounted at once, so replacing it would destroy xterm scrollback and unsaved editor buffers. `SystemState` itself *is* conditionally mounted (it holds no user state, and unmounting is what stops the poll). Open state is **hash-gated (`#system`) like the theme editor**, deliberately NOT in the server-persisted `ViewState`: `PUT /api/view` hard-coerces `{folder,sessionId}`, so a third field would need a protocol change + a `user_views` migration + re-reasoning the delicate restore-effect ordering — and restoring this page on another device would *hide* the folder you were working in. `replaceState` (not `pushState`) keeps it out of history and can't fire `hashchange` at `useThemeEditorOpen`.
  - **Gauges only ever draw against a REAL scale.** A bar needs a denominator, and inventing one puts the warn/danger colours at an arbitrary place. So: GPU power uses `power.limit`; SM clock uses `clocks.max.sm`; load uses `cpuCount` (24.0 is saturation on this box, a meltdown on a 2-core VM); temperatures use the sensor's own throttle point and **fall back to a plain row when none is published** (`TempMeter`) — `k10temp` exposes no `temp1_crit`/`temp1_max` here, so CPU temp legitimately has no bar while NVMe (crit 88.85°C) does. **`temperature.gpu.tlimit` is a MARGIN, not an absolute** ("GPU Current T.Limit Temp" = headroom), so the ceiling is `tempC + tlimit`; reading it as absolute paints an idle card as critical. Pinned by `gpu.test.ts`, plus an e2e invariant that a reported limit always exceeds its reading.
  - **`Bar` has a `tone`.** Default `load` colours warn ≥75% / danger ≥90%; `plain` never colours, for metrics where **high is good** (prefix-cache hit rate, clock-vs-max) which would otherwise glare red exactly when things are going well.
  - **Grid:** `repeat(auto-fit, minmax(min(320px,100%), 1fr))` with `max-width:1188px` and `align-items:start` (cards size to content rather than stretching to the tallest in the row). 320px is chosen so a 640px viewport (the app's only breakpoint) can't fit two columns — phones stay single-column *by arithmetic*, with **no second media query to keep in sync** — and 1188px caps the row at 3 (a 4th column would need 4×320+3×12 = 1316px). Cards land 320–388px wide; raising the cap to 4 is a one-number edit. All colours are existing theme vars (no new `:root` var, no `THEME_VARS` entry); bars use a `--raised` track (rising off the card's `--panel`, never a sunken `--bg` well). **Every list row (`.system-item`) is a `--raised` chip, separated by a 4px gap** of the card's `--panel` showing through — still no divider line. An *alternating* fill was tried first and read as "half the rows lost their styling"; it also hid the track of any bar landing on a filled row, which is why `.system-item .system-bar` re-tints (`color-mix` 12% `--text` off `--raised`, the same idiom as `.chat-tool-output`) — a bar can never use its own row's fill as its track.
- **Image uploads (chat prompts, claude + pi):** users attach images to a chat prompt via paste, a file-picker button (composer-inline when the mobile key-bar is down, in the key-bar when it's up — same `!keyboardOpen` swap as the `/` button, never both), or drag-drop onto the composer (`ChatView.tsx` — `attachments` state, `onPaste`/`onDrop`/hidden `<input type=file>`; thumbnails with per-item upload status; Send enabled on text **or** ≥1 ready image, blocked while any upload is in-flight). Transport is **REST-upload + reference** (NOT base64 over the WS), so `ChatState` snapshots stay light. Flow + **access control (enforced at all three points, keyed by the uploader's username via `authedUser`)**:
  - **Upload** — `client.uploadImage(file)` → `POST /api/upload` (auth'd; `readBinaryBody` cap `MAX_IMAGE_BYTES` 10 MB; `Content-Type` ∈ `ALLOWED_IMAGE_TYPES` {jpeg,png,gif,webp}). `server/uploads.ts` (`saveUpload`) writes bytes to `data/uploads/<randomUUID>` (path-confined) and records an ownership row in the `uploads` SQLite table (`server/db.ts`, pruned to 500). Returns `ChatImageRef {id, mediaType, name}` (never base64).
  - **Serve** — `GET /api/upload/:id` (auth'd) → `loadUpload(user, id)` returns bytes **only if the row's owner === the requesting user** (else 404 — no cross-user reads even with a guessed id). `<img src>` works via the same-origin HttpOnly cookie. Image `ChatPart` carries only `{id, mediaType, name, url:/api/upload/<id>}`.
  - **Use in a prompt** — the WS `chatAction` dispatch in `index.ts` (`resolvePromptImages`) loads each ref's base64 **only for images owned by the connection's user** (the `/ws` upgrade is auth-gated; the username is threaded into the connection), dropping unknown/foreign ids, then hands the augmented `ChatAction` to the manager/adapter. Harness mapping stays in the adapters: **claude** `buildUserContent()` builds Anthropic content blocks (`{type:"image",source:{type:"base64",media_type,data}}`); **pi** `piImages()` builds pi RPC `images:[{type:"image",data,mimeType}]` on the `prompt`/`steer` command (per pi's `docs/rpc.md`). Both echo a `user-message` whose parts come from the shared `promptParts(text, images)` (`shared/chat.ts`) so the transcript bubble shows text + image thumbnails; `render.ts` `renderPart` `case "image"` and `ChatView`'s user `Bubble` both render `<img class="chat-image">` (kept in sync). `manager.lastPromptLine` falls back to "🖼 image" for an image-only prompt. **Limitation:** resume rebuilds transcripts from harness on-disk history (text/thinking/tool only), so resumed user turns don't re-show their images (the agent's context still has them). Pure coverage: `shared/render.test.ts` (image part/bubble), `shared/chat.test.ts` (`promptParts` + reducer fold), `server/adapters/image-mapping.test.ts` (`buildUserContent`/`piImages`); the DB-backed ownership path is left to manual/e2e (importing `uploads.ts` would touch the real DB, so it's kept out of the pure suite).
- **Theming:** the palette is **12 CSS vars + a 3-step radius scale in `styles.css`'s `:root`** and nothing else — every color in the app resolves to one (`--bg`/`--panel`/`--raised`, `--text`/`--muted`, `--accent`/`--on-accent`, `--success`/`--warning`/`--danger`/`--danger-hover`/`--on-danger`). Adding a color = add a var + a `THEME_VARS` entry in `web/theme.ts`; **never hardcode a hex or a raw radius in a component or rule.** Only survivors are non-theme chrome: mask stencils in the auto-press ring's `linear-gradient(#000 0 0)`, white-on-scrim overlay bits (spinner, thumbnail badges, `.editor-preview-close` over arbitrary media), and depth shadows (`rgba(0,0,0,…)` — depth, not palette). `.theme-editor`'s own rules are also deliberately literal — it must stay readable while the palette is mid-edit.
  - **Elevation, not outlines** (the settled design idiom — don't regress it). **There is no border colour in the palette at all**: surfaces separate by three background steps, `--bg` < `--panel` < `--raised`, plus a shadow on floating things. **Nested content must rise, never sink** — a `--bg` fill inside a `--panel` card is the "sunken well" bug that made the old design read as dated; it kept recurring in *state* rules (hover/active/selected), so those use an accent tint (`color-mix(… var(--accent) 10–20%, var(--raised))`) rather than a darker fill. The one legitimate darkening is a momentary `:active` press.
  - The only surviving `border` declarations are **signal, never structure**: `.chat-request`'s accent ring (it blocks the turn), `.chat-plan`'s + `.chat-md blockquote`'s 3px left edge, `.assistant-warning`/`.chat-usage-plan`'s accent/danger rings, `.status-dot.editor`, and the spinner. Anything separating two same-coloured surfaces was replaced by an elevation step: dialog heads/feet (`.resume-dialog-head`, `.cmd-head`, `.cmd-foot`, `.editor-head`) went `--raised`, `.chat-commands` went `--raised` + an upward shadow, `.chat-tool-output` took a 5% `--text` tint. **Adding a divider is the wrong reflex — change the elevation instead.**
  - **Controls are filled, never outlined.** Every button/input/select/textarea/list-row is a `--raised` fill with no border. **Removing a `border` declaration from a control is not the same as having no border** — the UA stylesheet falls back to `border-style: outset`, i.e. a Windows-98 bevel. A base `button,input,select,textarea { border: none }` reset makes borderless the floor; never rely on merely deleting a rule. `select` additionally needs `appearance: none` (it otherwise draws native bevelled chrome and ignores background/border), which costs the dropdown arrow — redrawn by `.chat-model::after` as a **mask** filled with `var(--muted)` so it follows the palette (a `background-image` SVG would hardcode the colour, and a `background:` shorthand on the select would wipe it). This means **`--border` can no longer carry focus** — the old `border-color: var(--accent)` focus idiom (23 rules) was replaced by: `:focus` → a `box-shadow` ring (`0 0 0 2px` accent color-mix; a ring can't shift layout the way a border can), `:hover` → a 10% accent tint, `.active`/`.selected` → a 20% tint. **Any new control must keep a non-border focus indicator** — five rules pair `outline:none` with a ring and would go invisible if the ring were dropped.
  - **Radius scale is `--r-sm`/`--r-md`/`--r-lg` (4/8/12px).** Nested boxes must stay **concentric**: `inner = outer − padding`, so `--r-lg` wrapping 8px of padding takes `--r-sm` inside. Off-scale values make curves look non-parallel — the original bug was `--r-md` (8) nested inside a 12px bubble with 8px padding, where 4 was required. `.chat-request`'s padding is 8px 10px specifically to keep its nested `.chat-tool` concentric (the permission card is now the *only* nested tool — in the transcript a tool call is its own bubble at `--r-lg`).
  - `web/theme.ts` is the palette as data + `applyTheme`/`resetTheme`/`serializeTheme` + an `onThemeChange` pub-sub. The pub-sub exists for **xterm**, which takes colors as a JS object and can't follow CSS vars: `TerminalView` seeds `theme: xtermTheme()` and re-pushes on change (it used to hardcode a second copy of `--bg`/`--text`, which silently drifted).
  - `web/ThemeEditor.tsx` is a dev overlay: swatch + hex per color var (px slider for the `kind:"length"` radius vars), live-applied to `document.documentElement`, with a paste-ready `:root` block + Copy button (that's the export path — palettes ship by pasting into `styles.css`). Gated on the URL hash `#theme`, **not** `import.meta.env.DEV`, since the app normally runs as the prod systemd service — that's where the real sessions worth restyling are. Lazy-chunked; a `localStorage` draft (`DRAFT_KEY`, restored in `main.tsx` pre-render) keeps a palette across reloads but is a scratch buffer, never the source of truth.
  - **Named palettes** save/load to `localStorage` (`SAVED_KEY`, `listThemes`/`saveTheme`/`deleteTheme`), listed with a 4-swatch strip. Every read goes through `normalizeTheme(raw, base)`, which reconciles a stored palette against the *current* `THEME_VARS` — fills vars added since it was saved, drops retired ones, falls back on junk. Load it back raw and a palette written before `--raised` existed (or one still carrying `--border`) would apply a broken `:root`; the var list has already churned twice. Pure coverage `web/theme.test.ts`.
- **Command builder:** `./` key-bar button opens `CommandBuilder`. Lists recent/frequent (from the DB command log), cwd executables, then aliases/common/full-`$PATH` (server `GET /api/commands?cwd=` — auth'd, cwd must be a known folder). Picking a plain command then filters static subcommand/flag suggestions from `commandCatalog.ts` (free-text for unknown commands); picking a recent/frequent entry (a whole command line) inserts it directly. **Inserts** the assembled line into the PTY without a trailing newline — user reviews and presses Enter.
- **Recent/frequent commands:** sourced from the DB `commands` log (every command run in a Terminal session, with its cwd — see shell integration), not shell-history files. `recentCommands`/`frequentCommands` in `db.ts` rank distinct commands with the requested cwd preferred (same-cwd first, then global). Still filtered per-cwd in `listCommands`: only relative-path invocations (`./x`) are cwd-dependent and dropped if absent from the folder; plain names and absolute paths always kept. Ranked pool is larger than shown so the filter can still fill `HISTORY_LIMIT`. Cold-start: empty until commands are run through a Terminal session.
- **Shell integration / session events:** the `terminal` adapter (`adapters/shell.ts`) detects zsh/bash from the configured command and injects a startup script (zsh via hijacked `ZDOTDIR`+`USER_ZDOTDIR` → `shell-integration/zdotdir/`; bash via `--rcfile shell-integration/bash-rc.sh`) that still sources the user's own rc, then emits VS Code **OSC 633** markers (`E`=command line, `C`=exec, `D;code`=done, `P;Cwd=`=cwd). The adapter's `createEventParser()` strips those markers from the PTY stream and yields `SessionEvent`s; `manager.ts` updates `SessionInfo.cwd` (display/logging only — never re-parents the session, see above) and `SessionInfo.currentCommand` (set on `command-start`, cleared on `command-end`; shown in the UI session list with a pulsing dot) live and fans events to listeners (`onEvent`). Other harnesses (claude/pi) and non-zsh/bash shells get no parser — they stream raw, unchanged. Adding event support to a harness = give its adapter a `createEventParser`; keep the OSC knowledge in the adapter. Command logging lives in a single server-global `manager.subscribe` in `index.ts` (NOT the per-connection one, or every browser double-records).
- **Dynamic arg resolvers:** a catalog `ArgNode.source` names a server-side resolver (`RESOLVERS` in `commands.ts`) that runs a *fixed* command and returns live suggestions (e.g. `docker logs` → container names from `docker ps -a`; `git checkout` → branches; `npm run` → package.json scripts). Client calls `GET /api/resolve?id=&cwd=` with only the resolver **id** (never a command) — auth'd + folder-allowlisted; results cached 3s; failures degrade to free-text. Add a resolver = add an entry to `RESOLVERS` + set `source` on the catalog node.
- **Mobile keyboard:** `App.tsx` sizes `.app` to `visualViewport.height` when the keyboard is up and floats the key-bar above it; `index.html` sets `interactive-widget=resizes-content`. Together these stop the page being panned under the keyboard — don't revert without re-checking that.
- **Authentication:** the whole app is login-gated. `server/auth.ts` owns all of it — scrypt password hashing (`salt:hash`), server-side session tokens stored in SQLite (`users`/`auth_sessions` in `db.ts`) and set as an **HttpOnly cookie** (never exposed to page JS), plus the HTTP auth routes. The rest of the server only asks "who is this request?" via `authedUser()`; every `/api` route and the `/ws` upgrade are gated on it. `web/Login.tsx` is the login/signup gate and `web/auth.ts` the client helpers. Keep auth concerns in `auth.ts` — the rest of the code stays auth-agnostic.
- **LLM assist / AI-assistant mode (optional):** `server/llm.ts` talks to an **OpenAI-compatible endpoint** (`config.json` `llm` block; default local vLLM at `http://localhost:8000/v1`) to auto-answer chat `ui-request`s on the user's behalf — permission prompts (`kind:"select"`/`"confirm"` → allow/deny with a terse reason) and AskUserQuestion dialogs (`kind:"questions"` → pick option labels). Everything is **best-effort/fail-safe**: it polls the endpoint's `/models` for health, and any error/unreachable endpoint degrades to "unavailable" so the UI falls back to the normal manual flow. `server/llm.ts` (`evaluate`) is the pure, stateless judge; the mode itself is a **backend, per-session toggle** so it keeps running with **no browser open** (an unattended session doesn't stall):
  - **State:** `AssistantSettings` lives in each session's `ChatState` (`shared/protocol.ts`), toggled via the `set-assistant` `ChatAction` (handled by `manager.ts` itself, harness-agnostic — never the adapter) and folded by the reducer (`assistant-config` event); it's snapshotted on connect and synced to every client. **It's a checklist of independent capabilities**, each an `AssistantCapability` (`enabled` + its own free-text `instructions`, so they don't fight over one box): `permissions`, `questions` (+`onlyIfSure`), `autoPr` (+`autoMerge`), `continuity` (+`newSession`). `enabled` is a **derived** master switch — `deriveAssistantEnabled` (`shared/chat.ts`), recomputed by `manager.chatAction` on every `set-assistant` and **never trusted from a client**, so it can't drift from the checklist. **Defaults (`emptyChatState()`, mirrored verbatim by `DEFAULT_ASSISTANT` in `App.tsx` — keep the two in sync):** every capability's `enabled` is **off**, so the derived master is off and nothing is ever auto-answered until a box is ticked; but their *sub-options* ship pre-set to what a user ticking that capability almost always wants — `permissions.instructions = ALLOW_EVERYTHING`, `autoPr.autoMerge = true` and `continuity.newSession = "after-pr"`. Both are inert while their capability is off (`assistantNeedsLlm` on a fresh state is still false). Pinned by `shared/chat.test.ts`. `assistantNeedsLlm` (also `shared/chat.ts`) is the companion: questions and continuity always need the endpoint (continuity has no prompt to send without one), permissions need it unless blanket-accept, auto-PR never does — it drives the header button's colour and the dialog's unavailable-endpoint warning.
  - **Dialog (`web/App.tsx`, inline JSX):** the header robot button **always opens** the dialog (it no longer one-click disables — with several capabilities, editing them and "Run now" must stay reachable while the assistant is on; turning everything off is unticking the boxes). One `.assistant-section` per capability: a **bare checkbox** (enable/disable) beside a **separate `<button>`** carrying the label + chevron that toggles *only* the collapsible body — deliberately not a wrapping `<label>`, so the text can never flip the switch. All sections start **expanded** (both the initial state and each dialog open) so every option is visible at a glance; ticking auto-expands, unticking leaves it open. **There is no Save button**: checkbox changes push `set-assistant` immediately, instruction textareas debounce 300ms (flushed on close / "Run now"), and `assistantDraft` stays authoritative while open so an echo can't fight the caret. Sections take **no fill** — the dialog ladder is already `--bg`→`--panel`→`--raised` with no fourth step, so they group by spacing + the `[data-on]` accent tint and their `--raised` controls still rise (see the elevation rules below).
  - **Decider:** `server/assistant.ts` (`attachAssistant(manager)`, wired in `index.ts`) is a server-global subscriber and the **sole** evaluator: on a `ui-request` for an enabled+capable session it calls `evaluate()`, **broadcasts** the verdict as an `assistant-decision` event (`AssistantDecision` in `ChatState.autoDecisions`), then after the same grace delay the UI used (2–10s, scaled by card size) **applies** it via `manager.chatAction(ui-response)`. So the countdown ring renders identically in the browser, but the backend owns the timer/apply — closing the tab doesn't change the outcome. It passes the *matching* capability's instructions (`permissions` vs `questions`), plus `onlyIfSure` → `llm.ts` swaps `QUESTIONS_SYSTEM` for the stricter `QUESTIONS_SURE_SYSTEM` (both built from a shared `QUESTIONS_BASE` so they can't drift), and traces the system prompt actually sent.
  - **Intervention:** any card interaction sends `cancel-assistant` (→ `assistant-decision-cleared`, which the decider observes to cancel its timer) so a manual response wins; answering the card (→ `ui-request-done`) likewise cancels. `deny` never auto-acts (surfaced for a human, prefilled reason); `plan`/`input` are never auto-answered.
  - **Visibility (trace bubbles):** whenever the LLM is queried about a card, `evaluate()` returns a `trace` (`LlmDecision.trace` = the `SYSTEM`/`USER` prompt sent + the model's `reasoning_content` thoughts + raw response); the decider posts it via `manager.postAssistantTrace` → `assistant-trace` event → `ChatState.assistantTraces` (capped 20, reducer-folded). The reducer anchors each trace to the assistant turn it explains (`anchorMessageId` = the streaming message when the card fired — where the tool call lives; survives the streaming→finalized handoff, so it's clock-independent). `ChatView` renders each as a collapsible AI-mode bubble (glyph + one line + a chevron, expandable Prompt/Thoughts/Response) **inline right after its anchor turn**. **Nothing renders after the last message** — a trace parked at the end (the old orphan fallback) *stays* at the end, so every later message slides in above it and the transcript stops reading chronologically. That was the reported bug, and it's reachable fast: claude emits one `ChatMessage` per SDK assistant message, so an agentic turn is dozens, and the 200-message cap ages a note's anchor out within a few turns. Two halves: the reducer **validates a caller-supplied `anchorMessageId` against the live transcript** and re-anchors to the latest message when it's stale (a fresh note therefore always lands on a real turn), and `ChatView` **drops** a trace whose anchor has since aged out (like `rewind` already does) instead of flushing it to the end. A trace with *no* anchor predates every message (posted into an empty transcript) and renders first. Emitted for **every** deliberation incl. abstain/endpoint-error, so both allow and deny outcomes are auditable. Diagnostic/transient — not a `ChatMessage`, so it skips the render-log/resume; harness-agnostic (`AssistantTrace` in `shared/protocol.ts`). **The same array also carries plain AI-mode *notes*** from backend capabilities that consulted no LLM (`kind:"auto-pr"`, `outcome:"note"`, synthetic `requestId`, no `prompt`/`response`) — reusing it buys anchoring, capping and rewind-pruning for free, at the cost of sharing the 20-entry cap.
    - **What heads the line depends on whether an LLM spoke** (`deliberated` = `prompt || response`): a deliberation leads with the colored **verdict word** (`Allow`/`Deny`/…) — its summary would only restate it — while a **note leads with its `summary`**, with `reason` as a trailing muted detail. Getting this backwards is what printed "**Auto PR** origin" for what should read "Pushed joran/x to origin": the bubble rendered `outcome`+`reason` only, so every note's actual sentence was dropped and its incidental detail promoted (a `Branched x` note with no reason rendered as the bare word "Auto PR"). Hence: **a note's `summary` must be a self-contained sentence and `reason` must never carry the substance.**
    - Notes expand too — `AssistantTrace.detail` (long-form, e.g. a failed git step's whole stderr, since the first line regularly omits the cause; `autopr.ts`'s `failed()` helper pairs `firstLine()` as the reason with the full text as `detail`). The disclosure is gated on prompt/thoughts/response **or** `detail`, and a chevron advertises it.
    - Icon is per `kind`: the auto-PR pipeline gets Octicons' `git-merge` (its own 16px viewBox — hence `{box,d}` pairs), card deliberations keep the robot, so pipeline progress is distinguishable at a glance.
    - **Both kinds of link a note can carry.** URLs in `summary`/`reason`/`detail` are anchored by `ChatView`'s `<Linked>` over `web/linkify.ts` `linkRuns` (pure/tested: http(s) only — no bare-domain guessing, since it runs over machine text and stderr where a false positive is worse than a miss — trailing sentence punctuation trimmed, an unmatched `)`/`]` dropped but a balanced one kept). The anchors `stopPropagation`, because the line around them is the expand toggle. This is what makes auto-PR's `Opened PR #70 <url>` clickable without touching the server. **`AssistantTrace.sessionId`** is the other half: a note may name *another session* — `autopr.ts` fills it from `runPrSession`'s new `onSession` callback, so the "Drafting the pull request" note and every PR-agent **failure** note link to the spawned `/pr` tab (the failure path deliberately leaves that session open for a human, who otherwise had to find it by eye). `turnRouter.ts`'s `Note` takes it as `extra.session` — named apart from the origin `sessionId` it's posted to. `ChatView` renders an "open session ↗" chip only when the id is in the `knownSessions` set `App.tsx` passes (a stopped session keeps its tab, but a removed one has nothing to jump to), and `openSession` pins the tab under the **target's own** `folder` — same folder in practice, but a foreign pin would hide it (see the active-tab pin rule).
  - **Blanket-accept ("allow everything"):** when the session's `assistant.permissions.instructions` are exactly the sentinel `ALLOW_EVERYTHING` (`shared/chat.ts` `isAllowEverything`, case/trailing-punctuation tolerant), permission cards (`select`/`confirm`) skip `evaluate()` entirely and are auto-accepted — same grace-window countdown, same broadcast `assistant-decision`, same trace bubble (posted with a synthetic prompt/response noting no LLM was consulted), so the UX is identical. Needs no endpoint (`llmStatus()` gate is skipped); `questions` still go through the LLM; `plan`/`input` remain human-driven. The UI exposes it as an "allow everything" checkbox on the Instructions label row of the assistant dialog, which just writes/clears the sentinel — so typing the phrase yourself works identically (intentional). Pure coverage in `shared/chat.test.ts`; live coverage in `server/adapters/claude-assistant-allow-all.e2e.test.ts`, which **never starts the LLM poll** — on the normal path the decider bails on the `llmStatus()` gate, so a card auto-accepted there can only have come from the bypass (verified by mutation: disabling it hangs the turn).
  - **Auto PR (`server/autopr.ts` + `server/prAgent.ts` + `server/git.ts`):** the second capability, and now a *runner* rather than a subscriber: `runAutoPr(ctx, config)` is called by `server/turnRouter.ts` (below), which owns the settle hook, the single-flight and the note channel. On demand from the dialog's "Run now" the router calls it directly, ungated. **"Run now" travels as a `{type:"run-auto-pr"}` `ChatAction` observed via the `SessionListener.onChatAction?` hook** — *not* a no-op `ChatEvent`: `ChatEvent` means "renderable state changed" and `applyChatEvent` is the total pure function both sides run, so an inert event would smuggle an RPC onto the state-sync bus and ship dead bytes to every client. `manager.chatAction` fans the action to listeners and returns early for `run-auto-pr` so no adapter ever sees it (same "harness-agnostic actions never reach the adapter" rule as `set-assistant`/`set-draft`). A WS-layer special-case in `index.ts` would be wrong too — the `prompt`-images one exists only because image resolution is per-connection auth-scoped, and `chatAction` is called from the backend as well.
    - **Turn routing (`server/turnRouter.ts`, `attachTurnRouter(manager, config.autoPr)` — wired in `index.ts` beside `attachAssistant`/`attachSuggestions`):** `busy:false` carries **no stop reason** — the claude adapter clears busy on "success, error, or interrupt" alike — so structural checks can't tell a finished feature from a Stop-button abort, an unresolved error, or a turn that ended by asking a prose question (not a `ui-request`, so it looks idle). And *two* capabilities now want that moment, so the decision belongs to neither: the router renders `buildTurnDigest(state)` (last user request + last assistant message via `suggestions.ts`'s `messageText`, last 2k chars of each — a turn's verdict is in its closing lines) and asks `llm.ts` `routeTurn` for `auto-pr` | `continuity` | `none`, then calls `runAutoPr` or `runContinuity`. It also owns what both runners share: `shouldRouteTurn` (pure, tested — either capability on, idle, no pending card / queued prompt / **already-armed auto-prompt**, a real exchange happened), the per-session single-flight `Set` spanning the whole async run, the `anchors` map, and the `note()`/`failed()` helpers (`RunContext`). Deterministic pre-check: a transcript not ending in a non-empty **assistant** message → skip, no LLM call. **Fail-open only where it always was** — no endpoint / unusable reply routes to `auto-pr` when it's enabled (auto-PR never required the endpoint), else `none`; continuity can't run without it by definition. Every outcome posts an AI-mode note, the LLM ones carrying the `trace` so the verdict expands like a permission deliberation.
      - **A finished turn goes to `continuity` when auto-PR is off** — that's spelled out in `TURN_ROUTE_SYSTEM`, along with "do not answer `none` merely because the work is done". Without it the router dead-ends every completed turn and the loop stops after one iteration (observed live).
      - **The router arms on `user-message`, not on `busy:true`.** A `busy:true` → `busy:false` *transition* latch (what `prAgent.ts` uses) does NOT work here: a harness that doesn't stream partial messages — claude against a local endpoint sends no `message_start` — never emits `busy:true` at all, so the router would never fire. Found live; mutating the router back to a transition latch makes `claude-continuity.e2e.test.ts` fail outright, so that test is the regression. The prompt always happens, so it arms the latch: route at most once per prompt. (A *naive* `busy:false` match would also mostly work — `shouldRouteTurn`'s armed-auto-prompt check and the single-flight absorb pi's repeated settle — but it re-routes whenever a route finishes between the two, which arming removes structurally. Verified by mutation: the pi test does not catch that one.)
    - **Every note of a run is anchored to the turn that triggered it** (`anchors` map in `turnRouter.ts`, pinned at run start to the then-streaming/last message id and passed as `AssistantTrace.anchorMessageId`). A run takes minutes while the developer keeps chatting, so letting the reducer default the anchor (last message *at post time*) made the notes trail the live conversation — later messages rendered *above* them and they looked pinned under the transcript instead of embedded in it. The pin is honoured only while that turn is still in the transcript (the reducer re-anchors a stale one — see the trace-bubble bullet).
    - **The flow** (in `manager.sessionFolder(sessionId)`, each step narrating itself into the origin session's transcript as an AI-mode note): **1. branch** — only when on the integration branch: `llm.ts` `suggestBranchName(workingDiff)` → `git.ts` `sanitizeBranchName` → `<prefix>/<slug>`, deduped with a `-2`/`-3` suffix. **2. commit** — `git add -A` then up to 3 `suggestCommitMessage(stagedDiff)` attempts (each rejection fed back so the retry doesn't repeat it), validated by `sanitizeCommitMessage`, falling back to `auto-pr: update N files`. **3. push** — `git push -u origin <branch>`. **4. PR** — `prAgent.ts`, **unless the branch already has an open PR** (`git.ts` `openPrForBranch` → `gh pr list --head <branch> --state open`): a branch keeps its PR across turns, the step-3 push already updated it, and running `/pr` again would duplicate it or just fail. The existing `{number,url}` is adopted so step 5 can still merge it. `parseOpenPr` (pure, tested) reads the `--json` output; **`[]` (no PR) and unusable output both yield `null`** → fall through to creating one, i.e. missing/unauthenticated `gh` degrades to the pre-existing behaviour rather than silently never opening PRs. Merged/closed PRs don't count, so a reused branch can get a fresh one. **5. merge** and **6. back to main** (`git checkout <base>` + `git pull --ff-only`) run **only when `autoMerge` is ticked**; merge is `gh pr merge <n> --squash --delete-branch`. **7.** a landed merge then calls `runContinuity(ctx, {afterPr:true})` — a no-op when continuity is off, and the moment the `after-pr` new-session policy keys off; it runs inside the router's single-flight with the anchor still pinned, so it can't race a settle of its own. The pure `decideFlow({dirty,onMain,diffVsBase})` picks between `commit` / `pr-only` (clean branch that already diverges — push + PR, no new commit) / `nothing` (clean and matching the base — skip to step 6 regardless of `autoMerge`).
    - **Commits are made with `-c commit.gpgsign=false`.** Signing is *interactive* (`~/scripts/git-helper.sh` has a whole `prime_yubikey_auth` for it); headless it would hang forever, wedging the flow. Same reason `git.ts` sets `GIT_TERMINAL_PROMPT=0` on every invocation.
    - **`server/git.ts`** is the git/gh boundary: never-throwing `git()`/`gh()` wrappers (cwd-scoped, timed out) plus the **pure, tested** sanitizers the LLM output must survive — `derivePrefix` (ported from git-helper's `get_branch_prefix`: NFKD-decompose → drop `\p{M}` → first word → strip to `[a-z0-9._-]`, so `Jöran` → `joran`; falls back to the email local-part, then the OS user), `sanitizeBranchName` (lowercase, everything outside `[a-z0-9]` becomes a `-`, collapsed/trimmed, capped at 40 chars on a word boundary, `null` when nothing survives), `sanitizeCommitMessage` (`null` unless a single line ≤72 chars), `fallbackSlug`. `mainBranch` defers to `origin/HEAD` before guessing main/master. The LLM is **never trusted directly** — every generated name/message is sanitized or rejected, and an unavailable endpoint just falls through to the deterministic fallbacks (steps 1–3 still work with the LLM down; step 4 can't, and reports).
    - **The PR itself is drafted by a REAL chat session** (`prAgent.ts` `runPrSession`): `manager.start("pi", {cwd: folder})` — a normal, visible tab the user can watch or take over — prompted with `/pr` (both configurable via `config.json` `autoPr: {harness, command}`, so no harness id is hardcoded in the core). It speaks only `ChatAction`/`ChatState`, never pi's wire format. Its **permission cards are handled by turning blanket-accept assistant mode on for that session** (`set-assistant` with `permissions.instructions = ALLOW_EVERYTHING`) — reusing `assistant.ts` wholesale instead of adding a second card handler — and its `autoPr` is explicitly left off so the PR session can never open a PR about itself. Its **prose** questions ("does this description look good?") are answered by `llm.ts` `supervisePr(transcript)`, which reads the same `buildSuggestionTranscript` render and returns either a reply to send or `done` + the PR number/URL. Bounded by `MAX_PR_TURNS`/`MAX_RUN_MS`/`TURN_TIMEOUT_MS`; on success the session is stopped (its exited tab keeps the transcript), on any bound/failure it's deliberately **left open for a human**.
    - **A deliberate kill is not a failure:** `manager.kill()` sets `SessionInfo.stopped` (the single flag for "we killed it, it didn't die"), and the translator-chat `finish()` then skips the "surface the stderr tail as an error notice" path. SIGTERM yields a non-zero exit code, so a stopped session used to end its transcript with whatever the harness last wrote to stderr — for pi, its *startup* `--session-id` warning ("No project session found with id …"), which read as the PR session having been lost. Unattended kills (auto-PR success, Stop/Close) must end quietly; a harness that dies on its own still reports. The exited tab + its "Close session" button are unchanged. `stopped` also drives the session-row subtitle (`sessionMeta` in `App.tsx`): **"closed"** instead of `exited (143)` — a killed process exits non-zero *by definition*, so showing its code reads as a crash. It rides on `SessionInfo` (so a reconnect snapshot keeps it) **and** on the `exit` `ServerMessage`, filled in `index.ts` from `manager.sessionInfo(id)` — the `SessionListener.onExit` signature carries only the code and isn't worth widening for one flag. Coverage: `server/sessions/manager-exit.e2e.test.ts` (fake adapter over `node -e`, both directions — real subprocesses, but no harness/endpoint/tokens, hence e2e not the pure gate).
    - `prAgent.ts`'s own turn bookkeeping uses a `busy:true` → `busy:false` *transition* (its `armed` latch) — it drives the PR session directly and sends the prompt itself, so it doesn't need the router's `user-message` arming. pi emits several `busy:false` per turn, so a naive edge match there would settle the *next* prompt instantly. (`PiDriver` in the testkit gets away with the naive form only because a test awaits between turns.)
    - Known interaction: pi's `/pr` skill writes a `pr-description.md` into the working tree, which the *next* auto-PR run will happily commit. Not special-cased — hardcoding a harness skill's filename in harness-agnostic code is the wrong trade.
    - Coverage: `server/git.test.ts` (sanitizers/prefix/`parseOpenPr`, pure), `server/autopr.test.ts` (`decideFlow`, pure) and `server/turnRouter.test.ts` (`shouldRouteTurn` + `buildTurnDigest` + `firstLine`, pure). **Steps 1–3 are pinned live by `server/autopr.e2e.test.ts`** against a throwaway **local bare remote** (`git init --bare`) — no GitHub, no `gh`, no network, no harness, no tokens: it asserts the branch is cut as `<prefix>/<slug>` off main, the tree is committed with a single ≤72-char subject containing the new file, and the branch really reaches the bare repo (read back with `git -C <origin.git> rev-parse`). Step 4 is steered into its failure path by pointing `autoPr.harness` at a nonexistent id, which both covers the PR-agent failure report and stops the flow before anything needs `gh`. It runs with the LLM endpoint when one is up (so real model output goes through `sanitizeBranchName`/`sanitizeCommitMessage`) and falls back to the deterministic names when it isn't — the assertions hold either way, deliberately. **Steps 5–6 (merge, back to main) still have no e2e** — they need a real PR.
  - **Continuity Mode (`server/continuity.ts`):** the third backend capability — it keeps the agent working unattended. When `turnRouter.ts` routes a settled turn to `continuity`, `runContinuity(ctx, {afterPr})` reads the transcript, writes the developer's next message and arms it behind a countdown. The transcript is `buildSuggestionTranscript(state, {maxMessages:40, maxChars:12_000})` — the *same* renderer the suggestion chips use (now taking an options argument, defaults unchanged), which already consolidates tool calls to `[used tool: X]` and drops thinking, i.e. exactly "strip tool output, keep everything said".
    - **Start new session** (`ContinuityNewSession`, the dialog's nested `<select>`): `never` → always reply in place; `after-pr` (default) → clear only once auto-PR merged; `always` → that, plus whenever `llm.ts` `taskComplete(transcript)` judges the task done. Only the `always` branch spends that extra call. Pure `shouldStartNewSession(mode, {afterPr, taskComplete})`. Starting fresh: `manager.start(sameHarness, {cwd: folder})` → `set-assistant` with **the origin's whole checklist** (or the loop would end at the hop) → note → `manager.stop(origin)` (its tab keeps the transcript and reads "closed"). **Start before stopping**, so the folder is never momentarily empty; `App.tsx`'s auto-select-new-session effect brings the new tab forward. `llm.ts` `continuityPrompt(transcript, {fresh})` has a `fresh` variant because the new session has no memory of the old one — the message must stand alone.
    - **Plan rate limits gate the SEND, not the write** (claude only, in practice). After composing, `runContinuity` refreshes `ChatState.usage` (`{type:"usage"}` action → `usage` event) and runs the pure `usageBlocker(usage)`: if the **session** window (`five_hour`) or **any weekly** one (`seven_day*`, incl. the per-model variants) is at **≥90%**, it puts the prompt in the composer via the ordinary `set-draft` path, posts a `deny` note naming the worst offender + its reset time, and returns without arming. **Deliberately checked *after* the LLM calls**: composing is a local inference call (near-free), and `ChatState.draft` persists the result, so a held-back prompt is still worth having — the developer just presses Send when the window resets. Only the *unattended* send is withheld. It also stays in the CURRENT session — no fresh-session hop — since killing the running session while pausing would be a destructive surprise (a `fresh: true` prompt is self-contained, so it reads fine there too). The note carries **no `trace`** on purpose: attaching one would make the bubble lead with the verdict word and drop the summary (see the AI-mode note rules above).
      - **Fail-open, but only where it's honest**: `usage: null` (harness reports none — pi) and `available:false` (API-key / local / 3rd-party, where plan limits don't apply — this is why `claude-local` e2e runs aren't blocked) both proceed. But once a snapshot has *ever* arrived we know limits apply, so a timed-out refresh falls back to the stale value rather than waving the loop through blind. Windows with a null utilization can't block, and a window that is neither session nor weekly is ignored.
      - pi's translator no-ops the `usage` action (no data, no events, **no notice** — checked), so it simply times out. Since usage support is a property of the *harness*, one timeout is remembered in `usageUnsupported` — otherwise every lap of a pi loop would idle for the full `USAGE_TIMEOUT_MS`. claude-local answers instantly with `available:false`, so it never pays it.
    - **The countdown is the intervention point, and the BACKEND owns it** (same split as `assistant.ts`'s card verdicts, so it fires with no browser open). `ChatState.autoPrompt` (`AutoPrompt {id,text,delayMs,at}`) is folded by an `auto-prompt` event and cleared by `auto-prompt-cleared` — **id-matched**, so a stale clear can't kill a fresher prompt — plus by `user-message` (a prompt sent is an armed prompt spent) and `rewind`. The text also goes through the ordinary `set-draft` path, so it survives a reload and stays put to edit if withdrawn. `attachContinuity(manager)` is the canceller half: it drops its timer on `auto-prompt-cleared` (what the `cancel-auto-prompt` action folds to), on `user-message`, on `busy:true`, and on exit/removal. Timers are keyed by **prompt id** with a session→id index, because a run may arm on a *different* session than it read, and because the reducer has already dropped `autoPrompt` by the time the `user-message` listener runs.
    - **The delay emulates a human** reading the agent's last message and typing the reply, so the window scales with how much there is to take in: `autoPromptDelayMs(replyChars, promptChars)` = `replyChars/25 + promptChars/8` seconds (skim ~300 wpm, type ~95 wpm), clamped to **4–30 s**. Pure and tested. Reading time always comes from the *origin* session, even when the reply goes to a fresh one.
    - **UI** (`ChatView.tsx`): the armed text is force-adopted into the composer by a `useEffect` keyed on `autoPrompt.id` that bypasses the draft-echo `acked`/focus guards — this is explicit backend intent, not another client's keystrokes — and `.chat-send` gets `auto-press`. The ring CSS was generalised from `.chat-request-actions button.auto-press` to plain `button.auto-press` with `border-radius: var(--auto-radius, var(--r-md))` (`.chat-send.auto-press` overrides to `--r-lg`, staying concentric). **`--auto-elapsed` must be a custom property, not `animation-delay`** — the sweep runs on `::after`, and only custom properties inherit into a pseudo-element; it's set to a negative value so a client joining mid-countdown paints the arc it's actually at. Any composer control other than Send cancels on `pointerdown`, as does typing.
    - **No runaway bound, by design** — the loop runs until the box is unticked or a human intervenes. What keeps it sane: `shouldRouteTurn` refuses while a prompt is already armed, the router's single-flight, and the countdown itself. `prSessionAssistant()` in `prAgent.ts` pins `continuity.enabled:false` — same defence that stops the PR session opening a PR about itself.
    - **`llm.ts` background budget:** `routeTurn`/`taskComplete`/`continuityPrompt` go through `askTraced`, which uses `BACKGROUND_TIMEOUT_MS` (60 s) rather than the card evaluator's `EVAL_TIMEOUT_MS` (10 s). Nothing is blocked on them — the turn has already ended — and a local reasoning model routinely needs more than 10 s. Both timeouts were hit live before this was split.
    - Coverage: `shared/chat.test.ts` (fold, defaults, `deriveAssistantEnabled`/`assistantNeedsLlm`), `server/continuity.test.ts` (delay curve + `shouldStartNewSession` + `usageBlocker`, pure) and `server/continuity-usage.e2e.test.ts` (the over-limit branch, which a real harness can't reach without actually burning a plan to 90%: a **fake chat adapter** answers the `usage` request with any utilization, so the real manager/reducer/draft path runs — asserts under-limit arms, over-limit parks the prompt in the draft + posts a trace-less `deny` note naming the window, and that pausing never kills or replaces the session), and three live e2e files: `claude-continuity.e2e.test.ts` (the router arms a composed prompt with a delay inside the clamp, traces both deliberations, puts the text in the draft, and **sends it unattended** when the window closes; `cancel-auto-prompt` mid-window sends nothing and leaves the text to edit; and **the `after-pr` session hop** — calling `runContinuity(ctx,{afterPr:true})` directly makes `shouldStartNewSession` fire deterministically, so it pins the hop rather than a model opinion: fresh session in the same folder/harness, origin `stopped`, whole checklist carried, prompt armed on the NEW session) and `pi-continuity.e2e.test.ts` (the same loop over a **translator** harness — the harness-agnosticism proof — plus one-prompt-in-one-prompt-out against pi's repeated `busy:false`). The e2e enables `permissions` + `questions` too — the small local model reaches for tools whatever it's told and asks via `AskUserQuestion` rather than prose, so without them the turn never settles.

  - Harness-agnostic — lives outside adapters, driven from `index.ts`; protocol types (`LlmStatus`/`LlmEvaluateRequest`/`LlmDecision`/`AssistantSettings`/`AssistantCapability`/`AssistantDecision`/`AssistantTrace`/`AutoPrompt`/`ContinuityNewSession`) in `shared/protocol.ts`; gated on `llmStatus().available`. The conservative default (no user instructions) allows trivially-safe read-only operations **plus file edits/writes whose target path is inside the session's workspace folder or a subfolder, or under `/tmp/`** (`LlmEvaluateRequest.workspace` = `manager.sessionFolder`); writes outside the workspace and not under `/tmp/`, `..` escapes (to non-`/tmp/`), and destructive/networked shell ops are still denied. Live coverage: `server/adapters/claude-assistant.e2e.test.ts` drives a real edit turn through the manager with a listener that **never answers**, and asserts the backend evaluated → broadcast an `assistant-decision` → emitted an `assistant-trace` → applied it (`ui-request-done`) unattended. This is one **built** LLM feature; the others are auto-PR, Continuity Mode and the synthesized suggestion generator (next bullet); the rest below remain unbuilt.
- **Synthesized next-prompt suggestions (`server/suggestions.ts`, built):** harness-agnostic generator for chat harnesses that *don't* emit their own follow-up (pi + future ones — Claude does, so it's skipped via `HarnessAdapter.nativePromptSuggestions:true`). A server-global manager subscriber wired in `index.ts` (`attachSuggestions(manager, adapters)`): on a chat turn settling (`busy:false`) it renders the recent transcript (`buildSuggestionTranscript`, last ~10 msgs, tool calls as `[used tool: X]`, thinking omitted) and calls `llm.ts` `suggestNextPrompt()` (OpenAI-compatible endpoint, JSON `{suggestions:[…]}` — 1–3 *substantially distinct* prompts, deduped + capped at 3, fail-safe → `[]`), then `manager.postPromptSuggestions` folds a `prompt-suggestion` event into `ChatState.promptSuggestions` — the *same* chips the native harnesses feed (no UI change). Gated by `shouldSuggest` (idle, no pending card / queued / existing suggestions, real user+assistant exchange) + a per-session single-flight token so a new turn discards a stale in-flight prediction. Best-effort: no endpoint / empty transcript / decline → no chip. Deterministic gating+transcript pinned by `server/suggestions.test.ts` (pure); live path by `server/adapters/pi-suggestion.e2e.test.ts` (drives real pi through the manager against local vLLM, asserts a suggestion arrives + folds).

## What this is

- Harness-agnostic web remote for AI coding agents.
- Single webpage to manage local agent sessions: Claude Code + pi first, other CLI harnesses later.
- User-facing overview: `README.md`.

## Architecture (settled)

- **Frontend:** React + Vite. Renders sessions, streams output, sends input back.
- **Backend:** Node. Spawns each agent as a **local subprocess in a PTY** (`node-pty`), supervises lifecycle, streams raw terminal I/O to the browser via WebSocket.
- **Terminal:** browser renders sessions with **xterm.js**; keystrokes/resize go back over `/ws`. One persistent xterm per session (hidden when inactive) so scrollback survives tab switches.
- **Single port:** UI, `/api`, and `/ws` all listen on one port (default 4000). Dev embeds Vite in **middleware mode** in the Node server (`--dev` flag, not env); prod serves `dist/web`. `/ws` and Vite HMR coexist via `noServer` + manual `upgrade` routing.
- **Harness adapters:** core abstraction. An adapter describes *how to invoke* an agent (`HarnessInvocation`: command/args/env) plus optionally how to translate its protocol (`createEventParser` for PTY event markers, `createChatTranslator` for chat sessions); all process/streaming/lifecycle is harness-agnostic in `sessions/manager.ts`.
- **Hard constraint:** keep harness-specific logic confined to adapters. UI/session-mgmt/LLM features stay harness-agnostic. New agent = new adapter, never UI/core changes. Leakage into shared code = design smell.

## Config

- No env variables. All config lives in `config.json` (gitignored).
- `config-gen.sh` runs a setup dialog to generate `config.json`. Shape in `config.example.json`.
- Keys: `harnesses` (per-harness `enabled`/`command`, optional `env` for the process — used by `claudeLocal`), `llm` (`provider`/`baseUrl`/`model` for the optional LLM assist — see above), `autoPr` (optional `harness`/`command` for the auto-PR session, default `pi` + `/pr`), `server.port`.
- Never reintroduce `process.env`-style config or `.env` files.

## LLM features

- **Built:** AI-assistant mode — auto-answering chat permission/question dialogs, synthesized next-prompt suggestions, auto-PR (LLM-written branch name / commit message, plus supervision of the headless PR session), and Continuity Mode (routing each settled turn, then writing and sending the next prompt) via an OpenAI-compatible endpoint (`server/llm.ts`; see the LLM-assist bullet in Layout).
- **Planned (not built):** session summaries, natural-language task routing, notification/triage.
- All layered on the harness-agnostic core; consume the common session interface; must not depend on a specific harness.

## Positioning

- Open-source; README onboards external contributors.
- Contributor extension path: write new harness adapters.
